---
phase: 02-full-analytical-schema-source-spike
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/forexfactory/_query.py
  - src/forexfactory/__init__.py
  - src/forexfactory/cli.py
  - tests/test_query.py
  - tests/test_cli.py
autonomous: true
requirements: [DATA-05]

must_haves:
  truths:
    - "run_query default (include_no_data=False) returns data-bearing events plus holidays, but hides no-data speech events, per D-08 predicate hasDataValues == True OR impact == 'holiday'"
    - "run_query(include_no_data=True) surfaces speech/no-data events alongside everything else per D-09"
    - "Holiday events (impact=='holiday', hasDataValues False) remain visible by default per D-08 (holidays are first-class, not lumped with speeches)"
    - "forexfactory query --include-no-data surfaces speeches that are absent without the flag; forexfactory.get(include_no_data=True) mirrors it per D-12"
    - "forexfactory populate --force re-populates cached months (CLI surface for the 02-01 force= kwarg)"
    - "A query against a pre-Phase-2 parquet lacking the hasDataValues column logs a warning and does not raise (RESEARCH Pitfall 4)"
  artifacts:
    - path: "src/forexfactory/_query.py"
      provides: "include_no_data kwarg + default D-08 filter; PHASE2_COLUMNS empty-df fallback"
      contains: "include_no_data"
    - path: "src/forexfactory/__init__.py"
      provides: "get(include_no_data=False) passthrough"
      contains: "include_no_data"
    - path: "src/forexfactory/cli.py"
      provides: "--include-no-data (query) and --force (populate) flags + dispatch wiring"
      contains: "include-no-data"
    - path: "tests/test_query.py"
      provides: "QueryIncludeNoDataTests"
    - path: "tests/test_cli.py"
      provides: "include-no-data + force flag tests; skeleton fixture updated for hasDataValues"
  key_links:
    - from: "src/forexfactory/cli.py query dispatch"
      to: "_query.run_query"
      via: "include_no_data=args.include_no_data"
      pattern: "include_no_data=args\\.include_no_data"
    - from: "src/forexfactory/_query.py default filter"
      to: "hasDataValues column"
      via: "df[df['hasDataValues'] | (df['impact']=='holiday')]"
      pattern: "hasDataValues"
---

<objective>
Make no-data events first-class in the cache and filterable at query time (DATA-05).
Add an `include_no_data` kwarg + default predicate to `run_query()` (hide speeches by
default, always keep holidays — D-08), mirror it as `get(include_no_data=False)` and the
`--include-no-data` CLI flag (D-09/D-12), and expose the `--force` populate flag that wires
to the 02-01 rebuild kwarg. Update `_query`'s empty-df fallback to the shared
`PHASE2_COLUMNS`.

Purpose: After this slice a consumer sees data-bearing events + holidays by default and can
opt into speeches with one flag — the lean surface from D-09 — while the cache retains
everything.
Output: Filtered query layer, mirrored library/CLI surface, and tests proving the default
hides speeches, keeps holidays, and the flag surfaces speeches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md
@.planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md
@.planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md
@src/forexfactory/_query.py
@src/forexfactory/__init__.py
@src/forexfactory/cli.py
</context>

## Phase Goal

> Note: ROADMAP Phase-2 goal is artifact-shaped; user story derived from PROJECT.md Core Value.

**As a** consumer querying the cache, **I want to** get data-bearing events plus holidays by
default and opt into speech/no-data events with a single flag, **so that** I get analysis-ready
results without re-filtering, while the cache still retains everything.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add include_no_data default filter to run_query + get(); use PHASE2_COLUMNS fallback</name>
  <files>src/forexfactory/_query.py, src/forexfactory/__init__.py, tests/test_query.py</files>
  <read_first>
    - src/forexfactory/_query.py (_DATA01_COLUMNS line 30; run_query signature lines 110-117; concat/empty-df branch lines 171-176; currency+impact filter line 178)
    - src/forexfactory/__init__.py (get() signature lines 18-25; run_query passthrough lines 31-38)
    - src/forexfactory/_pipeline.py (PHASE2_COLUMNS — added in 02-01)
    - .planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md (Pattern 5 query-time default filter; Pitfall 4 stale-cache hasDataValues guard)
    - .planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md ("_query.py" + "__init__.py" sections; "Warn-and-Continue on Bad Data")
    - .planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md (D-08 default predicate, D-09 toggle, D-12 mirroring)
    - tests/test_query.py (helpers _make_parquet lines 22-25, _usd_high_row lines 28-36, QueryHappyPathTests._setup_cache lines 57-79 — REUSE these; note _usd_high_row has no hasDataValues key, so its parquets lack the column and the guard correctly skips the filter)
  </read_first>
  <behavior>
    - Given a cache month with one data-bearing USD/high row (hasDataValues True) and one USD/high speech row (hasDataValues False), run_query(currencies=["USD"], impacts=["high"]) default result contains the data-bearing row and NOT the speech row
    - run_query(..., include_no_data=True) result contains BOTH rows
    - Given a holiday row (impact "holiday", hasDataValues False), the default result CONTAINS it (D-08 holidays-always-visible)
    - Querying a parquet built without a hasDataValues column does not raise; result is returned (filter skipped, warning logged)
    - forexfactory.get(include_no_data=True) returns a Path; the kwarg reaches run_query
  </behavior>
  <action>
    In `_query.py`: import is already `from forexfactory import _cache, _pipeline`. Replace the `_DATA01_COLUMNS = [...]` definition and its single use in the empty-df fallback (line 175 `df = pd.DataFrame(columns=_DATA01_COLUMNS)`) with `_pipeline.PHASE2_COLUMNS` (use `pd.DataFrame(columns=_pipeline.PHASE2_COLUMNS)`). Remove the now-unused `_DATA01_COLUMNS` constant.
    Add `include_no_data: bool = False` to the keyword-only `run_query()` signature (before `cache_dir`). After the concat/empty-df branch and BEFORE the currency+impact filter (line 178), insert the default filter: `if not include_no_data:` then `if "hasDataValues" in df.columns:` apply `df = df[df["hasDataValues"] | (df["impact"] == "holiday")]`, else `logger.warning("[query] hasDataValues column absent — stale cache; run populate --force")` (warn-and-continue per the established pattern; never raise). Keep the existing currency+impact filter after it.
    In `__init__.py`: add `include_no_data=False` to `get()`'s keyword-only signature (after `end=None`) and pass `include_no_data=include_no_data` through to `_query.run_query(...)`. Keep the lazy `from . import _query` import.
    In tests/test_query.py: add a `_speech_row(...)` helper (hasDataValues False, impact "high", title "Fed Chair Powell Speaks") and a `_holiday_row(...)` helper (impact "holiday", hasDataValues False) following the existing `_usd_high_row` style — give the data-bearing `_usd_high_row`-style rows used here an explicit `hasDataValues: True`. Add a `QueryIncludeNoDataTests` class (reuse `_setup_cache`) with: test_default_hides_speeches, test_include_no_data_surfaces_speeches, test_holiday_visible_by_default, and test_stale_cache_without_hasdatavalues_does_not_raise (build a parquet from rows lacking the column and assert run_query returns a Path without raising). Do NOT modify QueryHappyPathTests — its rows lack hasDataValues so the guard leaves them untouched.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_query.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import forexfactory._query as q, inspect; assert 'include_no_data' in inspect.signature(q.run_query).parameters"` exits 0
    - `python3 -c "import forexfactory, inspect; assert 'include_no_data' in inspect.signature(forexfactory.get).parameters"` exits 0
    - `grep -n 'hasDataValues' src/forexfactory/_query.py` shows the default filter; `grep -c '_DATA01_COLUMNS' src/forexfactory/_query.py` returns 0 (constant removed)
    - `python3 -c "import forexfactory._query as q, forexfactory._pipeline as p; src=open('src/forexfactory/_query.py').read(); assert 'PHASE2_COLUMNS' in src"` exits 0
    - `python3 -m pytest tests/test_query.py -q` exits 0 (QueryIncludeNoDataTests + existing QueryHappyPathTests all green)
  </acceptance_criteria>
  <done>run_query hides speeches by default, keeps holidays, surfaces everything with include_no_data=True, degrades gracefully on stale cache; get() mirrors the kwarg; empty-df fallback uses PHASE2_COLUMNS; test_query.py green.</done>
</task>

<task type="auto">
  <name>Task 2: Add --include-no-data (query) and --force (populate) CLI flags + wiring</name>
  <files>src/forexfactory/cli.py, tests/test_cli.py</files>
  <read_first>
    - src/forexfactory/cli.py (populate subparser lines 75-105; query subparser lines 154-177; populate dispatch lines 207-221; query dispatch lines 223-240; D-12 flag/dispatch mirroring)
    - .planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md ("cli.py" section: store_true flag pattern, --force pattern, dispatch additions; "argparse Flag-to-Kwarg Mirroring (D-12)")
    - .planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md (D-09 store_true flag, D-12 mirroring)
    - tests/test_cli.py (SkeletonEndToEndTests _FIXTURE_DAYS lines 21-34 — the NFP event has NO hasDataValues key; after this slice the default query filter hides hasDataValues==False non-holiday rows, so this fixture MUST gain "hasDataValues": True to keep the end-to-end test green)
    - src/forexfactory/_populate.py (run_populate now accepts force= — from 02-01)
  </read_first>
  <action>
    In `cli.py` query subparser (after `--cache-dir`, ~line 177) add a `store_true` flag: `qry.add_argument("--include-no-data", action="store_true", default=False, help="Include speech/no-data events (default: data-bearing + holidays only) [D-09]")`. In the query dispatch `_query.run_query(...)` call (lines 226-232) add `include_no_data=args.include_no_data`.
    In `cli.py` populate subparser (after `--raw-dir`, ~line 105) add: `pop.add_argument("--force", action="store_true", default=False, help="Force rebuild all months even if already cached [Phase-2 migration]")`. In the populate dispatch `_populate.run_populate(...)` call (lines 209-216) add `force=args.force`.
    In tests/test_cli.py: (1) UPDATE `_FIXTURE_DAYS` so the NFP event dict includes `"hasDataValues": True` (it is a real data-bearing release) so SkeletonEndToEndTests still finds the row in the default query result. (2) Add a test that populates a month containing a speech row (hasDataValues False) plus a data-bearing row, runs `cli.main(["query", ...])` (default) and asserts the result parquet (read the printed path) lacks the speech, then runs `cli.main(["query", ..., "--include-no-data"])` and asserts the speech IS present. (3) Add a test that `cli.main(["populate", "--force", ...])` re-populates an already-cached month (populated path exercised) — or assert the argparse namespace carries force=True via a patch on `_populate.run_populate`. Follow the existing cli.main([...]) + redirect_stdout pattern.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `forexfactory query --help` (or `python3 -m forexfactory.cli query --help`) lists `--include-no-data`; `forexfactory populate --help` lists `--force`
    - `grep -c 'include_no_data=args.include_no_data' src/forexfactory/cli.py` returns 1; `grep -c 'force=args.force' src/forexfactory/cli.py` returns 1
    - A CLI end-to-end test shows a speech event absent in the default `query` result and present with `--include-no-data`
    - `python3 -m pytest tests/test_cli.py -q` exits 0 (SkeletonEndToEndTests green after the hasDataValues fixture fix)
  </acceptance_criteria>
  <done>Both CLI flags exist, are wired to their service kwargs, and the no-data flag demonstrably toggles speech visibility; test_cli.py green including the updated skeleton fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> service kwargs | User-supplied flags/strings cross into query/populate; month strings already validated by `_validate_month` |
| cached parquet -> query filter | Cache contents (possibly stale/pre-Phase-2) cross into the boolean filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | DoS/Info | `run_query` filter on stale cache | mitigate | `if "hasDataValues" in df.columns` guard logs a warning and skips the filter instead of raising KeyError (Pitfall 4). |
| T-02-04 | Tampering | CLI `--start/--end` injection into paths | accept | Pre-existing `_validate_month` + `_safe_token` already sanitize range/result-path tokens; this slice adds only boolean store_true flags (no new injectable strings). |
| T-02-SC | Tampering | package installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- `python3 -m pytest tests/ -q` passes (full suite — confirms query/CLI changes do not regress populate, refresh, scrape, docs).
- Manual: `forexfactory query --currency USD --impact high` and `forexfactory query --currency USD --impact high --include-no-data` against a populated cache print parquet paths whose row counts differ (speeches appear only with the flag).
</verification>

<success_criteria>
- Default query returns data-bearing events + holidays; speeches hidden (D-08).
- `--include-no-data` / `get(include_no_data=True)` surfaces speeches (D-09/D-12).
- Holidays remain visible by default.
- `--force` populate flag wired to `run_populate(force=...)`.
- Stale-cache query degrades gracefully (no crash).
- Full `pytest tests/` is green.
</success_criteria>

<output>
Create `.planning/phases/02-full-analytical-schema-source-spike/02-02-SUMMARY.md` when done.
</output>
