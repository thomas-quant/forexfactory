---
phase: 02-full-analytical-schema-source-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/forexfactory/_pipeline.py
  - src/forexfactory/_populate.py
  - src/forexfactory/_cache.py
  - tests/test_pipeline.py
  - tests/test_populate.py
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "flatten_events() yields the full analytical schema (raw value strings + parsed numerics + surprise flags + identity + hasDataValues) per D-01"
    - "_parse_value() converts FF value strings to float with percent-as-fraction and magnitude suffixes, returning float('nan') (never None, never raise) on unparseable input per D-02"
    - "A per-month parquet built by build_month_parquet() carries forecast_raw/actual_raw/previous_raw/revision_raw (str), forecast/actual/previous/revision (float64), actualBetterWorse/revisionBetterWorse/ebaseId/id (Int64), country (str), hasDataValues (bool)"
    - "Speech events are retained by build_month_parquet() (the should_keep_row 'speaks' drop is removed from the populate path) per D-09"
    - "run_populate(force=True) re-populates a month the manifest marks as cached; the manifest carries schema_version == '2'"
    - "FF UI/internal fields (checker, releaser, siteId, show*/enable*, notice, soloTitle, etc.) never appear in a flattened row per DATA-04"
  artifacts:
    - path: "src/forexfactory/_pipeline.py"
      provides: "_parse_value(), _NUMERIC_RE, _SUFFIX_MAP, PHASE2_COLUMNS constant, widened flatten_events()"
      contains: "PHASE2_COLUMNS"
    - path: "src/forexfactory/_populate.py"
      provides: "build_month_parquet widened (no should_keep_row, Int64 casts, PHASE2_COLUMNS empty-df fallback); run_populate force= kwarg + schema_version stamp"
      contains: "force"
    - path: "src/forexfactory/_cache.py"
      provides: "SCHEMA_VERSION constant"
      contains: "SCHEMA_VERSION"
    - path: "tests/test_pipeline.py"
      provides: "ParseValueTests + FlattenEventsWidenedTests"
    - path: "tests/test_populate.py"
      provides: "no-speaks-filter test, force=True test, Phase-2 dtype assertions"
  key_links:
    - from: "src/forexfactory/_populate.py"
      to: "src/forexfactory/_pipeline.py"
      via: "import PHASE2_COLUMNS for empty-df fallback"
      pattern: "_pipeline\\.PHASE2_COLUMNS"
    - from: "src/forexfactory/_pipeline.py flatten_events"
      to: "_parse_value"
      via: "parse each of the four raw value strings"
      pattern: "_parse_value\\("
---

<objective>
Widen the analytical schema at the single ETL choke point. Add a numeric value parser
(`_parse_value`), a shared `PHASE2_COLUMNS` constant, and grow `flatten_events()` from 7
fields to the full 19-field analytical schema (DATA-02/03/04). Update the populate path so
per-month parquets carry the wide schema with correct dtypes, retain speech events
(remove the `'speaks'` drop — D-09), and gain a `force=True` rebuild kwarg plus a manifest
`schema_version` stamp.

Purpose: This is the foundational slice every other Phase-2 thread builds on. After it,
a built per-month parquet contains forecast/actual/previous/revision (raw + parsed),
surprise flags, identity, and hasDataValues with correct, queryable dtypes.
Output: Widened `_pipeline.py`, `_populate.py`, `_cache.py`; new unit tests proving the
schema and parsing behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md
@.planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md
@.planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md
@src/forexfactory/_pipeline.py
@src/forexfactory/_populate.py
@src/forexfactory/_cache.py
</context>

## Phase Goal

> Note: the ROADMAP Phase-2 goal line is artifact-shaped, not a user story. The story below is
> derived from PROJECT.md Core Value (which is user-shaped) — see the planner's discrepancy note.

**As a** quant/research consumer of the shared cache, **I want to** read each calendar event's
forecast/actual/previous/revision (raw strings + parsed numerics), surprise flags, and identity
fields from the parquet, **so that** I can run expected-vs-surprise analysis from the cache
without re-scraping.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _parse_value + PHASE2_COLUMNS and widen flatten_events in _pipeline.py</name>
  <files>src/forexfactory/_pipeline.py, tests/test_pipeline.py</files>
  <read_first>
    - src/forexfactory/_pipeline.py (current CONFIG block lines 22-31; flatten_events lines 81-98; parse_json_to_csv lines 116-142 — note the fixed `cols` list at line 135 and the DictWriter at line 137)
    - .planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md (Pattern 1 _parse_value verified regex + the full edge-case table; Pattern 2 widened flatten_events 19-field yield; "float('nan') not None" pitfall)
    - .planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md ("_pipeline.py" section: constant placement, _parse_value placement, title fallback trimming soloTitle, id fallback "" -> None)
    - .planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md (D-01 column set, D-02 parsing rule)
    - tests/test_pipeline.py (PipelineLeakedFieldTests lines 77-144 — these call parse_json_to_csv and run_pipeline and MUST still pass)
  </read_first>
  <behavior>
    - _parse_value("4.3%") == 0.043 ; _parse_value("-10.7%") == -0.107 (percent divides by 100, sign preserved) [D-02]
    - _parse_value("-27.4K") == -27400.0 ; _parse_value("8.79M") == 8790000.0 ; _parse_value("2.0B") == 2000000000.0 ; _parse_value("1.89T") == 1890000000000.0 (suffix K/M/B/T)
    - _parse_value("50.8") == 50.8 (plain number passes through, no %)
    - math.isnan(_parse_value("")) , _parse_value("<0.10%") , _parse_value("Pass") , _parse_value("Yes") , _parse_value("1.34|2.6") all True (unparseable -> float('nan'), NEVER None, NEVER raise)
    - flatten_events on one event dict yields a dict whose keys are EXACTLY the 19 PHASE2_COLUMNS-source keys (date, time_utc, currency, impact, title, id, leaked, forecast_raw, actual_raw, previous_raw, revision_raw, forecast, actual, previous, revision, actualBetterWorse, revisionBetterWorse, ebaseId, country, hasDataValues) and contains NO dropped UI field (no "checker", no "soloTitle", no "siteId")
    - flatten_events row["forecast_raw"] is the verbatim FF string; row["forecast"] == _parse_value(row["forecast_raw"])
  </behavior>
  <action>
    Add module-level constants near the CONFIG block: `_NUMERIC_RE = re.compile(r'^([+-]?\d*\.?\d+)([KMBTkmbt%]?)$')` and `_SUFFIX_MAP = {'K': 1e3, 'M': 1e6, 'B': 1e9, 'T': 1e12, '%': 1e-2}`. Add `import re` and `import math` (math only if needed). Add `_parse_value(s: str) -> float` directly above flatten_events: strip input; empty/whitespace -> `float('nan')`; on regex no-match -> `float('nan')`; else `float(group1)` times `_SUFFIX_MAP[suffix.upper()]` when a suffix is present. It MUST NEVER raise (mirror the errors="coerce" posture per D-02). Return type annotation `float` (one-line docstring; this is a private helper but give it a docstring since the regex intent is non-obvious).
    Add the shared `PHASE2_COLUMNS` constant (a list, in the CONFIG section) in the FINAL parquet column order with `datetime_utc` first: `["datetime_utc", "currency", "impact", "title", "id", "leaked", "forecast_raw", "actual_raw", "previous_raw", "revision_raw", "forecast", "actual", "previous", "revision", "actualBetterWorse", "revisionBetterWorse", "ebaseId", "country", "hasDataValues"]`. This constant is imported by `_populate.py` and `_query.py` to prevent the three-way stale-column-list pitfall (RESEARCH Pitfall 3).
    Widen the `flatten_events()` yield to the 19-field dict from RESEARCH Pattern 2: keep date/time_utc/currency/impact/title; change `title` source to `ev.get("prefixedName") or ev.get("name") or ""` (DROP the `or ev.get("soloTitle")` term — soloTitle is in the DATA-04 drop list); change `id` fallback from `ev.get("id", "")` to `ev.get("id")` (None when absent, enabling Int64 casting downstream); add forecast_raw/actual_raw/previous_raw/revision_raw via `ev.get("<field>") or ""`; add forecast/actual/previous/revision via `_parse_value(<field>_raw)`; add `actualBetterWorse`/`revisionBetterWorse`/`ebaseId` via `ev.get(...)`; add `country` via `ev.get("country") or ""`; add `hasDataValues` via `ev.get("hasDataValues", False)`.
    REGRESSION GUARD: widening flatten_events adds keys the legacy CSV path does not expect. In `parse_json_to_csv()`, add `extrasaction="ignore"` to the `csv.DictWriter(...)` call (line ~137) so the legacy 7-column CSV does not raise `ValueError: dict contains fields not in fieldnames`. Do NOT change the legacy `cols` list — the legacy CSV path keeps its narrow shape. Leave `should_keep_row`, `run_pipeline`, and `csv_to_parquet` otherwise unchanged.
    In tests/test_pipeline.py append two classes following the existing unittest style: `ParseValueTests` (pure-function cases from the behavior block, using `math.isnan` for NaN cases and `assertAlmostEqual` for floats) and `FlattenEventsWidenedTests` (build one event dict with forecast="4.3%", actual="4.5%", previous="4.1%", revision="", actualBetterWorse=1, revisionBetterWorse=0, ebaseId=999, country="US", hasDataValues=True, id=12345; assert forecast_raw verbatim, forecast==0.043, presence of all 19 keys, and assert "checker"/"soloTitle"/"siteId" are NOT in the yielded dict).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import forexfactory._pipeline as p; import math; assert p._parse_value('4.3%')==0.043; assert p._parse_value('-27.4K')==-27400.0; assert math.isnan(p._parse_value('1.34|2.6')); assert math.isnan(p._parse_value(''))"` exits 0
    - `python3 -c "import forexfactory._pipeline as p; assert 'forecast_raw' in p.PHASE2_COLUMNS and 'hasDataValues' in p.PHASE2_COLUMNS and p.PHASE2_COLUMNS[0]=='datetime_utc'"` exits 0
    - `python3 -c "import forexfactory._pipeline as p; ev={'currency':'USD','impactName':'High Impact Expected','prefixedName':'CPI','dateline':1772368200,'id':1,'forecast':'4.3%','hasDataValues':True,'checker':'x'}; r=list(p.flatten_events([{'events':[ev]}]))[0]; assert r['forecast_raw']=='4.3%' and abs(r['forecast']-0.043)<1e-9 and 'checker' not in r and 'soloTitle' not in r"` exits 0
    - `python3 -m pytest tests/test_pipeline.py -q` exits 0 (existing PipelineLeakedFieldTests still pass — extrasaction="ignore" prevents the DictWriter regression)
  </acceptance_criteria>
  <done>_parse_value, PHASE2_COLUMNS, and the 19-field flatten_events exist; ParseValueTests + FlattenEventsWidenedTests pass; the full test_pipeline.py suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen build_month_parquet, add force= + schema_version to run_populate, add SCHEMA_VERSION</name>
  <files>src/forexfactory/_populate.py, src/forexfactory/_cache.py, tests/test_populate.py</files>
  <read_first>
    - src/forexfactory/_populate.py (build_month_parquet lines 37-88 — note the should_keep_row call lines 62-63, the empty-df fallback lines 66-68, the datetime construction lines 69-82; run_populate lines 95-225 — note signature lines 95-103, the skip-check lines 192-196, the final summary/return lines 221-225)
    - src/forexfactory/_cache.py (CONFIG block lines 22-25; update_manifest_month lines 128-161; write_manifest lines 105-121)
    - src/forexfactory/_pipeline.py (the PHASE2_COLUMNS constant added in Task 1)
    - .planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md (Pattern 3 empty-df fallback, Pattern 4 Int64 cast, Pattern 6 force-rebuild, "Cache Re-Population Sequencing" steps, Open Question 3 schema_version, Pitfall 6 should_keep_row stays in run_pipeline)
    - .planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md ("_populate.py" + "_cache.py" sections — exact line numbers and patterns)
    - tests/test_populate.py (PopulateHappyPathTests helpers _write_raw lines 20-27, _usd_high_event lines 29-37; test_parquet_has_data01_columns at line ~71 — this asserts EXACTLY the 6 DATA-01 columns and MUST be updated to the Phase-2 schema)
  </read_first>
  <behavior>
    - build_month_parquet on a months-worth of events produces a DataFrame whose columns include all of forecast_raw, forecast, actualBetterWorse, ebaseId, country, hasDataValues
    - df["forecast"].dtype == float64 ; df["hasDataValues"].dtype == bool ; df["actualBetterWorse"].dtype is Int64 ; df["ebaseId"].dtype is Int64 ; df["id"].dtype is Int64
    - A "Fed Chair Powell Speaks" event (hasDataValues=False) is RETAINED in the built parquet (no longer dropped) per D-09
    - run_populate(..., force=False) on an already-cached month returns skipped==1; run_populate(..., force=True) on the same month returns populated==1, skipped==0
    - After run_populate, the manifest read back has manifest["schema_version"] == "2"
  </behavior>
  <action>
    In `_cache.py` CONFIG block add `SCHEMA_VERSION: str = "2"` (one-line comment: Phase-2 wide analytical schema).
    In `_populate.build_month_parquet()`: DELETE the two-line `should_keep_row` filter (the `# Drop 'speaks' events` comment and the list-comprehension) — speeches are now retained in cache and filtered at query time (D-09). Do NOT touch `should_keep_row`'s definition in `_pipeline.py` or its use in `_pipeline.run_pipeline()` (Pitfall 6). Replace the empty-df fallback column list `["datetime_utc", "currency", "impact", "title", "id", "leaked"]` with `_pipeline.PHASE2_COLUMNS`. After the `pd.DataFrame(rows)` construction and the datetime_utc block (and before `write_parquet`), add the nullable-int cast loop over `INT_NULLABLE_COLS = ["id", "ebaseId", "actualBetterWorse", "revisionBetterWorse"]`, casting each present column with `.astype("Int64")` (RESEARCH Pattern 4 — guarantees nullable int parquet columns; never plain int64). Define `INT_NULLABLE_COLS` as a module-level constant in the CONFIG section.
    In `run_populate()`: add `force: bool = False` to the keyword-only signature (after `cache_dir`). Wrap the existing skip-check (lines ~192-196: the `cached_entry` lookup + `_scope_covers` + skip/continue) in `if not force:` so a force run falls through and rebuilds unconditionally. After the loop and before the final return, stamp the schema version: read the current manifest via `_cache.read_manifest(resolved_cache)`, set `manifest["schema_version"] = _cache.SCHEMA_VERSION`, and persist with `_cache.write_manifest(resolved_cache, manifest)` — guard with `if populated_count or skipped_count:` so an all-empty run does not create a bare manifest. The return dict shape stays `{"populated", "skipped", "empty"}` unchanged.
    In `cli.py` make NO changes here — the `--force` CLI flag is owned by plan 02-02 (cli.py file ownership).
    In tests/test_populate.py: (a) UPDATE the existing `test_parquet_has_data01_columns` to assert the Phase-2 schema instead of exactly-6 columns — assert presence of forecast_raw, forecast, actualBetterWorse, ebaseId, country, hasDataValues and the dtypes (forecast float64, hasDataValues bool, actualBetterWorse/ebaseId/id == "Int64"). Rename it to `test_parquet_has_phase2_columns` if helpful. (b) Add `_speaks_event()` helper (currency USD, impactName "High Impact Expected", name "Fed Chair Powell Speaks", dateline, id "powell-1", leaked False, hasDataValues False) and a test asserting a speaks event is RETAINED in the built parquet. (c) Add `test_force_true_overwrites_cached_month` following RESEARCH/PATTERNS: populate once (populated==1), populate again no force (skipped==1), populate with force=True (populated==1, skipped==0). (d) Add a test asserting the manifest has schema_version == "2" after a populate run.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_populate.py tests/test_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "from forexfactory import _cache; assert _cache.SCHEMA_VERSION=='2'"` exits 0
    - `grep -n 'should_keep_row' src/forexfactory/_populate.py | grep -v '^#'` returns NOTHING (the call is removed from the populate path); `grep -c 'def should_keep_row' src/forexfactory/_pipeline.py` returns 1 (definition retained)
    - `python3 -c "import forexfactory._populate as p; import inspect; assert 'force' in inspect.signature(p.run_populate).parameters"` exits 0
    - build-and-read smoke: a temp populate of one synthetic month yields a parquet where `str(df['forecast'].dtype)=='float64'`, `str(df['hasDataValues'].dtype)=='bool'`, `str(df['actualBetterWorse'].dtype)=='Int64'` (covered by the new tests)
    - `python3 -m pytest tests/test_populate.py tests/test_pipeline.py -q` exits 0
  </acceptance_criteria>
  <done>build_month_parquet emits the wide, correctly-typed schema and retains speeches; run_populate has a working force= path and stamps schema_version "2"; _cache.SCHEMA_VERSION exists; updated + new test_populate.py tests pass alongside test_pipeline.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped FF JSON -> flatten_events/_parse_value | Untrusted, attacker-influenceable event/value strings cross into typed parquet construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering/DoS | `_parse_value()` | mitigate | Bounded regex match only; no eval/exec; never raises; any unparseable/hostile string -> float('nan'). Magnitude expansion is a single bounded multiply (no suffix stacking possible). |
| T-02-02 | Tampering | `build_month_parquet` dtype enforcement | mitigate | Explicit `.astype("Int64")` and float64/bool inference reject malformed mixes loudly at write time rather than silently corrupting the typed schema. |
| T-02-SC | Tampering | package installs | accept | No new pip/npm/cargo installs introduced this phase (RESEARCH: Package Legitimacy Audit N/A). slopcheck not applicable. |
</threat_model>

<verification>
- `python3 -m pytest tests/ -q` passes (full suite — confirms no regression in the legacy CSV/pipeline paths, scrape, cache, query, cli, refresh, docs).
- `grep -v '^#' src/forexfactory/_pipeline.py | grep -c 'PHASE2_COLUMNS'` is >= 1 (constant defined).
- The legacy `parse_json_to_csv` path still writes its 7-column CSV (extrasaction="ignore" guard) — `tests/test_pipeline.py::PipelineLeakedFieldTests` green.
</verification>

<success_criteria>
- `_parse_value` passes all 14 verified edge cases (percent-as-fraction, K/M/B/T suffixes, signed, empty, `<0.10%`, `Pass`, `Yes`, pipe-separated -> NaN) and never raises.
- `flatten_events` yields the 19-field analytical schema; no DATA-04 UI field leaks.
- A built per-month parquet carries the wide schema with float64 / Int64 / bool dtypes and retains speeches.
- `run_populate(force=True)` rebuilds cached months and stamps `schema_version: "2"`.
- Full `pytest tests/` is green.
</success_criteria>

<output>
Create `.planning/phases/02-full-analytical-schema-source-spike/02-01-SUMMARY.md` when done.
</output>
