---
phase: 02-full-analytical-schema-source-spike
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - README.md
  - tests/test_docs.py
autonomous: false
requirements: [DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "After rebuild, a row LOADED FROM A CACHE PARQUET contains forecast_raw/actual_raw/previous_raw/revision_raw (str) alongside forecast/actual/previous/revision (float or null) — SC1"
    - "After rebuild, a row loaded from a cache parquet contains actualBetterWorse, revisionBetterWorse, ebaseId, and country — SC2"
    - "All on-disk months in out/ are rebuilt into the cache at schema_version '2' with NO re-scrape (zero HTTP)"
    - "After rebuild + spot-verification, the raw JSON staging layer is dropped (out/days_*.json and ~/.cache/forexfactory/raw/) — the Phase-1 D-03 exit condition"
    - "The README parquet-schema table documents the Phase-2 columns and test_docs.py asserts them"
  artifacts:
    - path: "README.md"
      provides: "Updated parquet-schema table covering Phase-2 columns + --include-no-data/--force CLI docs"
      contains: "forecast_raw"
    - path: "tests/test_docs.py"
      provides: "Doc-regression assertions for the Phase-2 schema columns"
      contains: "forecast_raw"
  key_links:
    - from: "rebuilt cache parquet"
      to: "Phase-2 schema"
      via: "forexfactory populate --force materializes flatten_events output"
      pattern: "forecast_raw"
---

<objective>
Materialize Phase 2 across the existing data and close the phase. Rebuild all ~195 on-disk
months into the cache at the wide schema (zero re-scrape), spot-verify SC1/SC2 on real
rebuilt parquets, document the new schema in the README/doc-regression test, and — only after
verification — drop the raw JSON staging layer (the Phase-1 D-03 exit condition). After this
slice the user's cache serves the full analytical schema for every cached month.

Purpose: This is the slice where DATA-02/03/04 become observably true at the cache level (a
row read from the parquet carries the analytical fields) and where the durable raw JSON layer
is retired per the locked exit condition.
Output: A rebuilt schema_version-"2" cache, updated docs, and the raw JSON staging removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md
@.planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md
@src/forexfactory/_populate.py
@src/forexfactory/_cache.py
@tests/test_docs.py
</context>

## Phase Goal

> Note: ROADMAP Phase-2 goal is artifact-shaped; user story derived from PROJECT.md Core Value.

**As a** consumer of the existing cache, **I want to** have every cached month carry the full
analytical schema (raw + parsed values, surprise flags, identity, hasDataValues), **so that** I
can do expected-vs-surprise analysis across all 195 months without re-scraping.

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the cache to the wide schema, spot-verify SC1/SC2, update docs</name>
  <files>README.md, tests/test_docs.py</files>
  <read_first>
    - .planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md ("Cache Re-Population Sequencing" Steps 1-7; spot-verify assertions; idempotency/rollback notes)
    - .planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md (JSON-drop exit condition carried forward from Phase-1 D-03)
    - src/forexfactory/_populate.py (run_populate force= + schema_version stamp — from 02-01)
    - src/forexfactory/_cache.py (SCHEMA_VERSION, month_parquet_path, manifest_path, raw_dir — to locate cache artifacts)
    - tests/test_docs.py (current schema assertions lines 36-46: the DATA-01 column rows + the exact `| `leaked` | boolean | ... |` line that MUST be preserved verbatim)
    - README.md (the existing parquet-schema table — find the DATA-01 rows to extend)
  </read_first>
  <action>
    Rebuild the cache from the on-disk raw JSON with NO network: run `forexfactory populate --force --raw-dir out` (uses the 02-01 force path + 02-02 CLI flag; default scope USD high/holiday per D-04). This overwrites every per-month parquet at the wide schema and stamps `schema_version: "2"` in the manifest. Expect ~195 months processed; high populated count; `empty` only for genuinely-empty months. Re-running is idempotent (RESEARCH: idempotency/rollback) so a partial/interrupted rebuild can be re-run safely.
    Spot-verify 3-5 rebuilt months with pandas: read e.g. `~/.cache/forexfactory/2024-03.parquet` (or other data-bearing months) and assert SC1 (forecast_raw/actual_raw/previous_raw/revision_raw object/str columns present AND forecast/actual/previous/revision float64 present) and SC2 (actualBetterWorse, revisionBetterWorse, ebaseId, country columns present); assert `str(df['hasDataValues'].dtype) == 'bool'` and `str(df['actualBetterWorse'].dtype) == 'Int64'`; assert at least one row has a non-null parsed forecast. Confirm `_cache.read_manifest(...)["schema_version"] == "2"`.
    Update README.md: extend the parquet-schema table with rows for every Phase-2 column (forecast_raw, actual_raw, previous_raw, revision_raw, forecast, actual, previous, revision, actualBetterWorse, revisionBetterWorse, ebaseId, country, hasDataValues) with short descriptions + dtypes; KEEP the existing DATA-01 rows and the exact `| `leaked` | boolean | Whether Forex Factory marked the event as leaked |` line unchanged (test_docs depends on it verbatim). Document the new `forexfactory query --include-no-data` and `forexfactory populate --force` flags in the CLI/usage section.
    Update tests/test_docs.py: extend `test_readme_schema_documents_current_parquet_columns` (or add a sibling test) to assert the README documents the Phase-2 columns — at minimum `"| `forecast_raw` |" in text`, `"| `forecast` |" in text`, `"| `actualBetterWorse` |" in text`, `"| `ebaseId` |" in text`, `"| `country` |" in text`, and `"| `hasDataValues` |" in text`.
    Do NOT delete any raw JSON in this task — deletion happens only after the human checkpoint (Tasks 2/3).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_docs.py -q && python3 -c "import pandas as pd, glob; ps=sorted(glob.glob('$HOME/.cache/forexfactory/2024-*.parquet')) or sorted(glob.glob('$HOME/.cache/forexfactory/*.parquet')); assert ps, 'no rebuilt parquets — run populate --force first'; df=pd.read_parquet(ps[0]); req=['forecast_raw','actual_raw','previous_raw','revision_raw','forecast','actual','previous','revision','actualBetterWorse','revisionBetterWorse','ebaseId','country','hasDataValues']; missing=[c for c in req if c not in df.columns]; assert not missing, missing; assert str(df['forecast'].dtype)=='float64' and str(df['hasDataValues'].dtype)=='bool' and str(df['actualBetterWorse'].dtype)=='Int64'; print('SC1/SC2 OK', ps[0])"</automated>
  </verify>
  <acceptance_criteria>
    - `forexfactory populate --force --raw-dir out` rebuilds the cache with zero HTTP; populated count covers the on-disk months
    - A rebuilt parquet read with `pd.read_parquet` contains all SC1 columns (4 raw str + 4 parsed float64) and all SC2 columns (actualBetterWorse, revisionBetterWorse, ebaseId, country)
    - `df['forecast'].dtype == float64`, `df['hasDataValues'].dtype == bool`, `df['actualBetterWorse'].dtype == Int64`
    - `_cache.read_manifest(cache_dir)["schema_version"] == "2"`
    - `python3 -m pytest tests/test_docs.py -q` exits 0 (README documents the Phase-2 columns; the verbatim `leaked` line preserved)
  </acceptance_criteria>
  <done>The full cache is rebuilt at the wide schema (schema_version "2") with zero re-scrape; SC1/SC2 verified on real parquets; README + test_docs.py document the Phase-2 schema. No raw JSON deleted yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm rebuild before the irreversible raw-JSON drop</name>
  <files>~/.cache/forexfactory/manifest.json (read-only verification of the rebuilt cache)</files>
  <what-built>
    Task 1 rebuilt every on-disk month into the cache at the Phase-2 schema (schema_version "2")
    and verified SC1/SC2 on sample parquets. The next step drops the raw JSON staging layer
    (out/days_*.json and ~/.cache/forexfactory/raw/) per the locked Phase-1 D-03 exit condition.
    This deletion is IRREVERSIBLE: out/*.json is the user's only on-disk copy of ~195 scraped
    months and is NOT tracked in git. After the drop, obtaining a new field requires a re-scrape
    (accepted per D-03).
  </what-built>
  <how-to-verify>
    1. Confirm the cache rebuild reported a healthy populated count and few/zero unexpected empties.
    2. Spot-check a couple of rebuilt months yourself, e.g.:
       `python3 -c "import pandas as pd; df=pd.read_parquet('$HOME/.cache/forexfactory/2024-03.parquet'); print(df[['datetime_utc','title','forecast_raw','forecast','actual','hasDataValues','ebaseId','country']].head())"`
    3. Confirm the rebuilt parquets carry the analytical fields with sensible values.
    4. Approve the irreversible drop of out/days_*.json and ~/.cache/forexfactory/raw/, OR ask to keep
       the raw JSON (e.g., copy it to a backup first).
  </how-to-verify>
  <resume-signal>Type "approved — drop raw JSON" to proceed with deletion, or describe a concern (e.g., "back up out/ first").</resume-signal>
  <read_first>
    - .planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md (D-03 carried-forward exit condition + the user's strong preference against durable JSON)
    - .planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md ("Cache Re-Population Sequencing" Step 5 — exactly what to delete)
  </read_first>
  <action>
    PAUSE for human confirmation BEFORE any deletion. Surface the rebuild summary (populated/empty
    counts) and the SC1/SC2 spot-verify result from Task 1. The deletion of out/days_*.json and
    ~/.cache/forexfactory/raw/ is irreversible (out/*.json is not in git) — proceed to Task 3 only on
    explicit approval ("approved — drop raw JSON"). If the user asks to back up out/ first, wait for them
    to do so (or copy out/ to a backup path they specify) before continuing.
  </action>
  <verify>
    <automated>python3 -c "import json,os; m=json.load(open(os.path.expanduser('~/.cache/forexfactory/manifest.json'))); assert m.get('schema_version')=='2', m.get('schema_version'); print('rebuild verified: schema_version=2, months=', len(m.get('months',{})))"</automated>
  </verify>
  <acceptance_criteria>
    - The rebuilt manifest reports schema_version "2" and a populated months map (rebuild is real before the gate)
    - The human has confirmed the rebuild + verification look correct
    - Explicit approval (or an alternative instruction such as "back up first") is recorded before any deletion
  </acceptance_criteria>
  <done>The human has reviewed the rebuilt cache and explicitly approved (or amended) the irreversible raw-JSON drop; no deletion has happened yet.</done>
</task>

<task type="auto">
  <name>Task 3: Drop the raw JSON staging layer (phase exit)</name>
  <files>README.md, tests/test_docs.py</files>
  <read_first>
    - .planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md ("Cache Re-Population Sequencing" Step 5: rm out/days_*.json and rm -r ~/.cache/forexfactory/raw/)
    - tests/test_docs.py (the project-structure chart test asserts README still references `out/` and `days_YYYY_MM.json` — see note below)
  </read_first>
  <action>
    After the Task 2 approval, drop the raw JSON staging layer per the Phase-1 D-03 exit condition:
    delete the legacy repo-root staging with `find out -name 'days_*.json' -delete` (removes the ~195 days_YYYY_MM.json files), and remove the cache raw staging dir with `rm -rf ~/.cache/forexfactory/raw` (note: this dir is created empty by ensure_dirs and is only populated by `refresh`, so it is typically empty after a populate-only rebuild — removing it is still correct per the exit condition).
    DOC-REGRESSION GUARD: tests/test_docs.py::test_project_structure_chart_uses_plain_ascii_and_matches_repo_layout asserts the README structure chart still references `out/` and `days_YYYY_MM.json`. Because out/ is now emptied, update the README project-structure chart so it no longer claims live `days_*.json` files are present (or document out/ as the optional re-scrape staging target), AND update that test_docs assertion to match the new reality — keep the doc-regression honest rather than asserting a now-false layout. If you keep an `out/` reference, frame it as "optional raw-staging dir (populated on re-scrape)".
    Do not touch the rebuilt per-month parquets or manifest.json (those are the durable cache and must remain).
  </action>
  <verify>
    <automated>test -z "$(find out -name 'days_*.json' 2>/dev/null)" && test ! -d "$HOME/.cache/forexfactory/raw" && python3 -m pytest tests/test_docs.py -q && echo JSON_DROPPED_DOCS_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `find out -name 'days_*.json'` returns nothing (legacy raw JSON dropped)
    - `~/.cache/forexfactory/raw/` no longer exists
    - The per-month cache parquets + manifest.json remain intact (`ls ~/.cache/forexfactory/*.parquet` non-empty; manifest schema_version "2")
    - `python3 -m pytest tests/ -q` exits 0 (test_docs updated to the new repo reality; no other regression)
  </acceptance_criteria>
  <done>The raw JSON staging layer is dropped (phase exit), the durable parquet cache + manifest remain, and the doc-regression test reflects the new layout. Full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| irreversible deletion of out/*.json | The only on-disk raw copy (not in git) is destroyed — a data-integrity boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Denial/Data-loss | raw JSON drop | mitigate | Deletion is gated behind a blocking human-verify checkpoint that only proceeds AFTER a full rebuild + SC1/SC2 spot-verification; the human may opt to back up out/ first. The decision to drop is locked (D-03); the checkpoint protects the timing. |
| T-02-11 | Tampering | rebuilt cache correctness | mitigate | populate --force is idempotent and re-runnable; spot-verify asserts SC1/SC2 + dtypes + schema_version before any deletion (RESEARCH rollback note). |
| T-02-SC | Tampering | package installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- `forexfactory populate --force --raw-dir out` rebuilds the cache with zero HTTP; manifest schema_version == "2".
- A rebuilt parquet satisfies SC1 (raw + parsed value columns) and SC2 (surprise + identity columns) with correct dtypes.
- `python3 -m pytest tests/ -q` passes (incl. updated test_docs.py).
- After approval: `find out -name 'days_*.json'` is empty and `~/.cache/forexfactory/raw/` is gone; durable parquets + manifest remain.
</verification>

<success_criteria>
- SC1: a cache-parquet row carries forecast_raw/actual_raw/previous_raw/revision_raw (str) + forecast/actual/previous/revision (float/null).
- SC2: a cache-parquet row carries actualBetterWorse, revisionBetterWorse, ebaseId, country.
- All on-disk months rebuilt at schema_version "2" with no re-scrape.
- Raw JSON staging dropped after verification (Phase-1 D-03 exit).
- README + doc-regression test document the Phase-2 schema; full `pytest tests/` green.
</success_criteria>

<output>
Create `.planning/phases/02-full-analytical-schema-source-spike/02-05-SUMMARY.md` when done.
</output>
