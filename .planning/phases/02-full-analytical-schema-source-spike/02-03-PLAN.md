---
phase: 02-full-analytical-schema-source-spike
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/form1_rich_month.html
  - tests/fixtures/form2_bracket_no_data.html
  - tests/fixtures/empty_month.html
  - tests/fixtures/multi_candidate.html
  - tests/test_scrape.py
autonomous: true
requirements: [QUAL-05]

must_haves:
  truths:
    - "tests/fixtures/ contains realistic HTML fixtures covering both calendarComponentStates assignment forms (= {...} and [n] = {...}), a rich data-values month, a no-data speech/holiday case, an empty month, and a multi-candidate _select_best_days case per D-10"
    - "Each fixture embeds REAL FF event field values sourced from out/*.json (trimmed to a representative day slice) per D-11"
    - "At least one fixture-based test feeds a saved HTML fragment into extract_days() and asserts correct event output (currency, impact source, title, id, raw forecast string, hasDataValues, leaked) per SC4/D-11"
    - "The empty-month fixture yields zero events; the multi-candidate fixture yields the richest days set"
    - "Fixtures exercise the fragile JS-quirk branches: unquoted object keys (_quote_js_object_keys) and single-quoted string values (_replace_single_quoted_strings)"
  artifacts:
    - path: "tests/fixtures/form1_rich_month.html"
      provides: "= {...} whole-object assignment, data-bearing events"
    - path: "tests/fixtures/form2_bracket_no_data.html"
      provides: "[n] = {...} bracket assignment, speech/holiday events (hasDataValues False)"
    - path: "tests/fixtures/empty_month.html"
      provides: "zero-events month"
    - path: "tests/fixtures/multi_candidate.html"
      provides: "multiple state objects; _select_best_days must pick the richest"
    - path: "tests/test_scrape.py"
      provides: "ExtractDaysFixtureTests"
  key_links:
    - from: "tests/test_scrape.py ExtractDaysFixtureTests"
      to: "tests/fixtures/*.html"
      via: "Path(__file__).parent / 'fixtures' / name"
      pattern: "fixtures"
    - from: "ExtractDaysFixtureTests"
      to: "_scrape.extract_days"
      via: "parse fixture HTML"
      pattern: "extract_days\\("
---

<objective>
Protect the project's highest-risk component — the character-by-character
`calendarComponentStates` HTML/JS parser (`extract_days()` and its helpers) — with
fixture-based regression tests (QUAL-05). Build a small fixture matrix (D-10) from REAL FF
event data trimmed out of `out/*.json` (D-11), covering both assignment forms, rich/no-data/
empty/multi-candidate cases, and the fragile JS-quirk branches; add an
`ExtractDaysFixtureTests` class asserting correct parsed event output.

Purpose: Today a site-side JS change silently makes `extract_days()` return `[]` with no test
to catch it (CONCERNS: highest-priority coverage gap). These fixtures turn that into a loud
test failure.
Output: 4 HTML fixtures + a fixture-driven test class; SC4's fixture requirement satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md
@.planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md
@.planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md
@.planning/codebase/CONCERNS.md
@src/forexfactory/_scrape.py
</context>

## Phase Goal

> Note: ROADMAP Phase-2 goal is artifact-shaped; user story derived from PROJECT.md Core Value.

**As a** maintainer of the scraper, **I want to** have the fragile JS-state parser covered by
fixture-based regression tests over realistic FF HTML, **so that** a site-side bundle change
fails CI loudly instead of silently returning zero events.

<tasks>

<task type="auto">
  <name>Task 1: Build the realistic HTML fixture matrix from real out/ event data</name>
  <files>tests/fixtures/form1_rich_month.html, tests/fixtures/form2_bracket_no_data.html, tests/fixtures/empty_month.html, tests/fixtures/multi_candidate.html</files>
  <read_first>
    - src/forexfactory/_scrape.py (the parser: _extract_state_json lines 212-234 for the `= {...}` form; _extract_assigned_state_objects lines 237-252 + _extract_days_array_from_state_object lines 274-285 for the `[n] = {...}` form with the unquoted `days:` key; _quote_js_object_keys line 148; _replace_single_quoted_strings lines 153-205; _select_best_days lines 255-271; extract_days lines 288-298)
    - .planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md (D-10 fixture matrix, D-11 real captured HTML + trimming + targeted assertions)
    - .planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md ("tests/fixtures/" section: naming, trimming convention, file load pattern)
    - tests/test_scrape.py (the existing inline-HTML test lines 42-57 shows the exact `<script> window.calendarComponentStates = {...}` shape and the bracket-form test that follows — mirror these shapes in the fixtures)
    - A real data source: read 1-2 files under out/ (e.g. `out/days_2024_01.json`, plus a month containing speeches/holidays) to copy REAL event field values; trim to 1-3 days of events per fixture
  </read_first>
  <action>
    Source real events: read `out/days_2024_01.json` (rich data-bearing month per RESEARCH) and one month containing speech and/or holiday events (grep out/*.json for `"hasDataValues":false` or titles containing "Speaks"/"Holiday"); copy a small representative slice (1-3 days) of REAL event dicts into the fixtures so the field VALUES (forecast/actual strings, currency, impactName, id, hasDataValues, leaked) are authentic (D-11). Keep each fixture small.
    Write `tests/fixtures/form1_rich_month.html`: a `<script>` block with the whole-object form `window.calendarComponentStates = { "month": { days: [ ...real data-bearing events... ] } };`. To exercise the fragile branches, use UNQUOTED object keys (e.g. `days:` not `"days":`) and at least one SINGLE-QUOTED string value (e.g. a title in single quotes) so `_quote_js_object_keys` and `_replace_single_quoted_strings` are exercised. Include events with non-empty forecast/actual/previous and hasDataValues true.
    Write `tests/fixtures/form2_bracket_no_data.html`: the bracket form `window.calendarComponentStates[1] = { days: [ ...real speech/holiday events with hasDataValues false... ] };` (this path goes through `_extract_assigned_state_objects` + `_extract_days_array_from_state_object`, which requires the unquoted `days:` key).
    Write `tests/fixtures/empty_month.html`: a valid state object whose days array is empty (or days with empty events lists) so `extract_days` returns a structure with zero events.
    Write `tests/fixtures/multi_candidate.html`: multiple state objects (e.g. `"short"` with few/empty days and `"month"` with many event-bearing days) so `_select_best_days` must pick the richest (most days + most events). Mirror the shape of the existing inline test at tests/test_scrape.py lines 42-57.
    Each file: wrap the script in minimal surrounding `<html>`/`<script>` tags only (no need for full page chrome). Keep total fixture size small (trimmed).
  </action>
  <verify>
    <automated>python3 -c "import forexfactory._scrape as s; from pathlib import Path; d=Path('tests/fixtures'); 
print('form1', sum(len(x.get('events',[])) for x in s.extract_days((d/'form1_rich_month.html').read_text()))); 
print('form2', sum(len(x.get('events',[])) for x in s.extract_days((d/'form2_bracket_no_data.html').read_text()))); 
print('empty', sum(len(x.get('events',[])) for x in s.extract_days((d/'empty_month.html').read_text()))); 
print('multi', sum(len(x.get('events',[])) for x in s.extract_days((d/'multi_candidate.html').read_text())))"</automated>
  </verify>
  <acceptance_criteria>
    - All four fixtures exist under tests/fixtures/ and are non-empty files
    - `extract_days()` on form1_rich_month.html returns >=1 event with a non-empty raw `forecast` string and `hasDataValues` True
    - `extract_days()` on form2_bracket_no_data.html returns >=1 event with `hasDataValues` False
    - `extract_days()` on empty_month.html returns zero total events
    - `extract_days()` on multi_candidate.html returns the richest day-set (>1 event), proving _select_best_days selected the best candidate
    - form1 exercises an unquoted key and a single-quoted string value (so _quote_js_object_keys + _replace_single_quoted_strings run without error)
  </acceptance_criteria>
  <done>Four realistic, trimmed fixtures exist; extract_days parses each correctly through both assignment forms and the multi-candidate selector.</done>
</task>

<task type="auto">
  <name>Task 2: Add ExtractDaysFixtureTests in test_scrape.py</name>
  <files>tests/test_scrape.py</files>
  <read_first>
    - tests/test_scrape.py (imports lines 1-8; existing ScrapeTests inline-HTML tests lines 34-57 — copy assertion style)
    - .planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md ("tests/test_scrape.py" section: the `_fixture()` loader using Path(__file__).parent / "fixtures" / name; "Pattern 7" in RESEARCH for the test-class skeleton)
    - .planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md (D-11 targeted field assertions)
    - tests/fixtures/*.html (the four fixtures from Task 1)
  </read_first>
  <action>
    Append an `ExtractDaysFixtureTests(unittest.TestCase)` class to tests/test_scrape.py. Add a `_fixture(self, name)` helper returning `(Path(__file__).parent / "fixtures" / name).read_text(encoding="utf-8")`. Tests (assert on the RAW event output of `extract_days` — this is the parser's contract per SC4, independent of the schema-widening work):
    - test_form1_whole_object_assignment_rich_month: load form1_rich_month.html; days = extract_days(html); assert len(days) > 0; pick a data-bearing event (hasDataValues True) and assert keys currency, impactName (or impactTitle), prefixedName-or-name, id, and a non-empty raw forecast string are present; assert hasDataValues is True.
    - test_form2_bracket_assignment_no_data: load form2_bracket_no_data.html; assert >=1 event with hasDataValues False (speech/holiday).
    - test_empty_month_returns_no_events: load empty_month.html; assert total event count == 0.
    - test_multi_candidate_selects_best_days: load multi_candidate.html; assert total event count > 1 (the richest candidate, not the sparse one).
    Use plain unittest assertions matching the existing file style. Do NOT assert parsed-numeric values here (parsed numerics are covered by FlattenEventsWidenedTests in plan 02-01) — keep this class a pure parser-regression suite so it stays independent of the schema slice.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_scrape.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_scrape.py::ExtractDaysFixtureTests -q` exits 0 with >=4 tests
    - At least one test feeds a saved HTML fixture into extract_days() and asserts correct event output (SC4)
    - `python3 -m pytest tests/test_scrape.py -q` exits 0 (existing ScrapeTests still green)
  </acceptance_criteria>
  <done>ExtractDaysFixtureTests passes over all four fixtures; SC4's fixture-based extract_days test requirement is satisfied; existing scrape tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved HTML fixture -> extract_days() | Test-only; exercises the parser against attacker-shaped JS structures (the real-world untrusted surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | `extract_days` parser robustness | mitigate | Fixtures pin the parser's behavior across both assignment forms + JS quirks so a regression (returning [] on a structure change) fails CI; this is the QUAL-05 mitigation for the highest-risk component. |
| T-02-06 | Info disclosure | fixture repo size / PII | accept | Fixtures trimmed to 1-3 days of public economic-calendar data (no secrets/PII); D-11 trimming keeps the repo light. |
| T-02-SC | Tampering | package installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_scrape.py -q` passes (fixture tests + existing parser tests).
- `python3 -m pytest tests/ -q` passes (no regression elsewhere).
- Optional synergy: if the 02-04 spike captures real live FF page HTML, it MAY be added as an additional fixture — but this plan does not depend on it (kept autonomous so QUAL-05/SC4 are satisfied without network).
</verification>

<success_criteria>
- Fixture matrix (D-10) exists with real trimmed FF data (D-11).
- ExtractDaysFixtureTests asserts correct extract_days output across both assignment forms, empty, and multi-candidate cases (SC4).
- Both JS-quirk branches (unquoted keys, single-quoted strings) are exercised.
- Full `pytest tests/` is green.
</success_criteria>

<output>
Create `.planning/phases/02-full-analytical-schema-source-spike/02-03-SUMMARY.md` when done.
</output>
