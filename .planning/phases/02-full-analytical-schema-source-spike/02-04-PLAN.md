---
phase: 02-full-analytical-schema-source-spike
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/forexfactory/_api.py
  - src/forexfactory/_refresh.py
  - .planning/PROJECT.md
  - .planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md
autonomous: false
requirements: [SRC-01]
user_setup:
  - service: forexfactory.com (public, no account)
    why: "Live browser DevTools reconnaissance of the apply-settings POST endpoint (D-07)"
    dashboard_config:
      - task: "Open https://www.forexfactory.com/calendar in a browser with DevTools Network tab; click month prev/next; capture the apply-settings POST (URL, method, request headers, cookies, body/params, response JSON)"
        location: "Browser DevTools → Network"

must_haves:
  truths:
    - "A documented decision exists in PROJECT.md Key Decisions stating whether the apply-settings endpoint replaces the HTML parser as primary or remains a documented fallback (SC5) — true regardless of the spike outcome per D-04"
    - "The decision records the D-06 four-part adopt-bar result (no field regression; arbitrary historical months back to 2010; works via curl_cffi no-auth; stable at a polite rate) for each criterion"
    - "The shipped fetcher remains curl_cffi only — no browser/devtools/Playwright recon tooling is added to pyproject.toml per D-07"
    - "If adopted: _api.py is wired into _refresh.run_refresh as the primary source with HTML-scrape-and-parse (SRC-02) as the documented fallback; if rejected: _api.py is removed or clearly marked throwaway and HTML parse stays primary"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Resolved SRC-01 decision in the Key Decisions table (SC5)"
      contains: "apply-settings"
    - path: ".planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md"
      provides: "Captured request transaction + D-06 evaluation evidence"
  key_links:
    - from: ".planning/PROJECT.md Key Decisions"
      to: "SRC-01 outcome"
      via: "documented decision row"
      pattern: "apply-settings|SRC-01"
---

<objective>
Run the SRC-01 spike: investigate the FF `apply-settings` POST endpoint as a cleaner
structured-data source. Perform live reconnaissance (D-07), reproduce the request precisely
with `curl_cffi` (no browser in the shipped path), diff its output against the HTML parser
across a month sample, evaluate the four-part adopt-bar (D-06), and write a documented
decision (SC5) on whether it replaces or remains a fallback to the HTML parser.

Purpose: Reduce reliance on the fragile char-by-char JS parser IF a reliable structured
endpoint exists. The deliverable that always lands is the documented decision (SC5) — adoption
is conditional on clearing the D-06 bar.
Output: A documented decision in PROJECT.md, spike evidence notes, and (if adopted) a wired
`_api.py` primary fetcher; otherwise HTML parse stays primary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md
@.planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md
@.planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md
@src/forexfactory/_scrape.py
@src/forexfactory/_refresh.py
</context>

## Phase Goal

> Note: ROADMAP Phase-2 goal is artifact-shaped; user story derived from PROJECT.md Core Value.

**As a** maintainer, **I want to** know whether the FF apply-settings POST endpoint is a
reliable structured source that can replace the fragile HTML parser, **so that** I have a
documented, evidence-backed source decision (and a primary fetcher if it wins).

<tasks>

<task type="auto">
  <name>Task 1: Automated curl_cffi reconnaissance of the apply-settings endpoint</name>
  <files>.planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md</files>
  <read_first>
    - src/forexfactory/_scrape.py (build_session lines 301-305 — REUSE; HEADERS lines 42-53; scrape_month retry/headers template lines 308-341; ff_month_token lines 78-80)
    - .planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md ("SRC-01 Spike Research", "Spike Decision Protocol", Pitfall 7 navigation parameter, Open Questions 1, Assumptions A1-A3)
    - .planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md (D-04, D-05, D-06 adopt-bar, D-07 live recon + constraint that recon tooling must not ship)
    - .planning/PROJECT.md (§Context: the spike lead URL https://www.forexfactory.com/calendar/apply-settings/100000?navigation=1)
  </read_first>
  <action>
    Using `_scrape.build_session()` + curl_cffi Chrome impersonation, do an automated first pass: GET `https://www.forexfactory.com/calendar` to obtain cookies/session; capture the response cookies and any CSRF token embedded in the page or headers; grep the page's embedded JS for how the `apply-settings` request is formed (the URL `/calendar/apply-settings/100000`, the `navigation` param, the request body/params, any token). Then attempt a `session.post("https://www.forexfactory.com/calendar/apply-settings/100000", params={"navigation": "1"}, ...)` with the inferred headers/body and record the HTTP status, response content-type, and a snippet of the response body/shape. Explicitly attempt at least one historical month (e.g. a 2015 or 2018 target token) to probe Pitfall 7 (does navigation reach arbitrary history or only the current ±window?).
    Record everything to `.planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md`: the exact request(s) tried, status codes, response shapes, and which D-06 criteria look promising vs blocked (e.g. CSRF/session needs that curl_cffi could/couldn't satisfy). REDACT any live cookie/token values (record only their presence/shape, never the secret).
    If there is NO network access in this environment: record that explicitly in the spike notes and mark the automated recon as "deferred to live human recon" — do NOT fabricate results. The human checkpoint (Task 2) and the documented decision (Task 3) still proceed.
    Do NOT add any recon tooling to pyproject.toml. curl_cffi is already a dependency; nothing new is shipped.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md && grep -qi 'apply-settings' .planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 02-SRC01-SPIKE.md exists and records the attempted request(s), observed status/shape (or an explicit "no network — deferred" note)
    - The notes state, per D-06 criterion, what was observed or what remains undetermined (field completeness, historical access, curl_cffi/no-auth feasibility, rate stability)
    - No recon tooling added to pyproject.toml (curl_cffi remains the only network dependency)
  </acceptance_criteria>
  <done>Automated curl_cffi recon attempted and documented (or explicitly deferred on no-network), with per-criterion observations recorded in the spike notes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live DevTools capture of the real apply-settings request (D-07)</name>
  <files>.planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md</files>
  <what-built>
    Task 1 attempted the apply-settings POST automatically with curl_cffi and recorded findings
    (or a no-network deferral) in 02-SRC01-SPIKE.md. The faithful capture of the REAL request the
    FF JavaScript issues requires a browser DevTools Network capture, which is a human-only action.
  </what-built>
  <how-to-verify>
    1. Open https://www.forexfactory.com/calendar in a browser with DevTools → Network tab open.
    2. Click the month prev/next navigation arrows.
    3. Find the POST to /calendar/apply-settings/100000 (filter on "apply-settings").
    4. Copy: the full request URL + query params, request method, request headers (especially Cookie,
       Content-Type, and any X-Csrf-Token), the request body/form params, and the response body + its
       JSON shape (does it contain forecast/actual/previous/revision, actualBetterWorse,
       revisionBetterWorse, ebaseId, country, hasDataValues?).
    5. If possible, repeat for a historical month (e.g. 2015) to confirm arbitrary historical access (D-06 #2).
    6. Paste these details so they can be reproduced exactly with curl_cffi; OR, if you cannot/do not
       want to run live recon now, say "skip live recon — keep HTML parser primary" and the spike will
       document the endpoint as investigated-but-not-adopted (SC5 still satisfied).
  </how-to-verify>
  <resume-signal>Paste the captured request/response details, or type "skip live recon — keep HTML parser primary".</resume-signal>
  <read_first>
    - .planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md (Task 1 findings to confirm/correct)
    - .planning/phases/02-full-analytical-schema-source-spike/02-CONTEXT.md (D-06 adopt-bar, D-07 recon)
  </read_first>
  <action>
    PAUSE for human live reconnaissance. Present Task 1's automated findings to the user. On resume,
    append the human-captured request/response transaction (URL, method, headers, cookies-as-shape,
    body/params, response JSON shape) — or the explicit "skip live recon" decision — to
    02-SRC01-SPIKE.md so Task 3 can reproduce the request precisely. REDACT live cookie/token VALUES
    (record presence/shape only). Recon tooling stays throwaway and is never added to pyproject (D-07).
  </action>
  <verify>
    <automated>grep -qiE 'captured|response|skip live recon|deferred' .planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md && echo CAPTURE_OR_SKIP_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - The captured (or explicitly skipped) request details are appended to 02-SRC01-SPIKE.md
    - If captured: enough detail exists (URL, method, headers, body, response shape) to reproduce the request with curl_cffi
    - If skipped: the notes record that live recon was declined and HTML parser remains primary
  </acceptance_criteria>
  <done>The real request is captured (reproducible with curl_cffi) or live recon is explicitly skipped; the outcome is recorded in 02-SRC01-SPIKE.md.</done>
</task>

<task type="auto">
  <name>Task 3: Prototype _api.py, evaluate D-06 adopt-bar, write the documented decision (SC5)</name>
  <files>src/forexfactory/_api.py, src/forexfactory/_refresh.py, .planning/PROJECT.md, .planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md</files>
  <read_first>
    - .planning/phases/02-full-analytical-schema-source-spike/02-SRC01-SPIKE.md (Task 1 + Task 2 captured request/response)
    - .planning/phases/02-full-analytical-schema-source-spike/02-PATTERNS.md ("_api.py (NEW)" section — module structure modeled on _scrape.py: docstring, curl_cffi try/except ImportError guard, logger, CONFIG constants APPLY_SETTINGS_URL/IMPERSONATE/REQUEST_TIMEOUT, REUSE _scrape.build_session(), fetch_month_api mirrors scrape_month with session.post)
    - .planning/phases/02-full-analytical-schema-source-spike/02-RESEARCH.md (Pattern 8 spike fetcher; "Spike Decision Protocol"; D-06 criteria)
    - src/forexfactory/_scrape.py (build_session, scrape_month, extract_days for the comparison baseline)
    - src/forexfactory/_refresh.py (run_refresh lines 44-161 — the integration point IF adopted: the scrape_month call at line 116 is where an API fetcher would slot in as primary with scrape_month as fallback)
    - .planning/PROJECT.md (Key Decisions table row "Investigate FF JSON/POST endpoint..." to resolve)
  </read_first>
  <action>
    IF the endpoint is reproducible (Task 1/2 yielded a working request): create `src/forexfactory/_api.py` modeled on `_scrape.py` — module docstring, `try/except ImportError` curl_cffi guard, `logger = logging.getLogger(__name__)`, a CONFIG block with `APPLY_SETTINGS_URL`/`IMPERSONATE`/`REQUEST_TIMEOUT`, and `fetch_month_api(session, *, month_token, ...)` that REUSES `_scrape.build_session()` and mirrors `scrape_month`'s retry/raise_for_status structure but with `session.post(...)`. Return a days list shaped like `extract_days` output so it is source-agnostic (D-05). Then run the comparison: for 3-5 sample months INCLUDING at least one historical month (e.g. 2015 or 2018 per Pitfall 7), call both `_scrape.scrape_month` (HTML) and `fetch_month_api`, and diff the event field sets — confirm presence of forecast/actual/previous/revision, actualBetterWorse, revisionBetterWorse, ebaseId, country, hasDataValues. Evaluate all four D-06 criteria.
    DECISION (UNCONDITIONAL — this is SC5 and must happen on every outcome):
    - Append the D-06 four-criteria result (pass/fail/undetermined) and the adopt/reject conclusion to 02-SRC01-SPIKE.md.
    - Update the `.planning/PROJECT.md` Key Decisions table: resolve the existing "Investigate FF JSON/POST endpoint..." row from "Pending — Phase 2 (SRC-01 spike)" to the outcome — ADOPTED (apply-settings is primary, HTML parse is the SRC-02 fallback) or NOT ADOPTED (HTML parse remains primary; endpoint investigated, reason recorded).
    - IF ADOPTED: wire `_api.fetch_month_api` into `_refresh.run_refresh` as the primary fetch with `_scrape.scrape_month` as the fallback when the API returns empty/errors; keep `_api.py` in the package. The shipped fetcher stays curl_cffi only (no browser/Playwright) per D-07.
    - IF NOT ADOPTED (including the no-network / skipped-recon case): do NOT wire anything into _refresh.py; remove `src/forexfactory/_api.py` if it was created as a throwaway prototype (or leave it absent), and ensure HTML parse remains primary. The documented decision still lands in PROJECT.md (SC5 satisfied either way).
    NEVER add browser/devtools/Playwright tooling to pyproject.toml under any outcome.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -q && grep -Eqi 'apply-settings|SRC-01' .planning/PROJECT.md && echo DECISION_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md Key Decisions records a resolved SRC-01 outcome (adopted or not-adopted) — SC5 satisfied
    - 02-SRC01-SPIKE.md records the D-06 four-criteria evaluation
    - If adopted: `python3 -c "import forexfactory._api as a; assert hasattr(a, 'fetch_month_api')"` exits 0 and _refresh.run_refresh uses it as primary with scrape_month fallback; if not adopted: HTML parse remains primary and no _api import is wired into _refresh
    - `grep -ci 'playwright\|selenium\|nodriver' pyproject.toml` returns 0 (no recon tooling shipped)
    - `python3 -m pytest tests/ -q` exits 0 (no regression; if adopted, refresh tests still pass with the fallback path)
  </acceptance_criteria>
  <done>A documented SRC-01 decision exists in PROJECT.md (SC5) backed by D-06 evidence; the package ships curl_cffi only; if the endpoint cleared the bar it is the wired primary with HTML fallback, otherwise HTML remains primary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FF apply-settings response -> _api parser | Untrusted external JSON crosses into event-dict construction |
| recon tooling -> shipped package | A spike-only browser/devtools tool must NOT cross into the shipped dependency set (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | `_api.fetch_month_api` JSON handling | mitigate | Defensive `response.json()` + shape checks; return [] on missing keys; mirror scrape_month's retry/raise_for_status; never eval. Fixed FF URL (no SSRF). |
| T-02-08 | Info disclosure | captured cookies/CSRF in spike notes | mitigate | Treat session cookies/tokens as secrets — do not commit live cookie values into source or PROJECT.md; keep them only as transient request shape in the spike notes (redact tokens). |
| T-02-09 | Tampering (supply chain) | recon tooling | mitigate | Recon browser/devtools/Playwright is throwaway dev-only; verified NOT added to pyproject.toml (grep gate). Shipped fetcher is curl_cffi only. |
| T-02-SC | Tampering | package installs | accept | No new SHIPPED pip installs; any recon-tool install is a transient dev install excluded from pyproject (RESEARCH: Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `python3 -m pytest tests/ -q` passes.
- `.planning/PROJECT.md` Key Decisions has a resolved SRC-01 row (SC5).
- `grep -ci 'playwright\|selenium\|nodriver' pyproject.toml` == 0.
- The shipped `refresh` path either uses the adopted API primary with HTML fallback, or HTML primary unchanged.
</verification>

<success_criteria>
- SC5: a documented decision on apply-settings exists in PROJECT.md (adopt or fallback) — satisfied for both outcomes (D-04).
- D-06 four-criteria evaluation recorded with evidence.
- Recon tooling never ships; shipped fetcher is curl_cffi only (D-07).
- Full `pytest tests/` is green.
</success_criteria>

<output>
Create `.planning/phases/02-full-analytical-schema-source-spike/02-04-SUMMARY.md` when done.
</output>
