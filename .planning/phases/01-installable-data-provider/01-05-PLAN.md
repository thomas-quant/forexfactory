---
phase: 01-installable-data-provider
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - src/forexfactory/cli.py
  - tests/test_cli.py
autonomous: true
requirements: [PKG-02, CACHE-01, CACHE-02, DATA-01]
must_haves:
  truths:
    - "PKG-02 / SC2: `forexfactory populate` dispatches to run_populate and re-processes on-disk months with zero HTTP"
    - "PKG-02 / SC3: `forexfactory query --currency USD --impact high` prints an absolute path to stdout that opens with `pd.read_parquet`"
    - "D-10: `query` writes ONLY the path to stdout; all diagnostics go to stderr/logging (so `PARQUET=$(forexfactory query ...)` works)"
    - "D-12: scope is passed via repeatable singular flags `--currency`/`--impact` (argparse append), mirroring the library list args"
    - "D-09: the CLI catches the out-of-scope ValueError from run_query and exits non-zero with the guidance on stderr"
    - "D-04 / CACHE-02: omitting scope flags applies the USD + high/holiday default; CACHE-01: `--cache-dir` overrides the cache home"
  artifacts:
    - path: "src/forexfactory/cli.py"
      provides: "Unified `forexfactory` console script: populate + query subcommands + dispatch"
      contains: "def main"
    - path: "tests/test_cli.py"
      provides: "Routing, append-flag, path-only-stdout, and the walking-skeleton end-to-end test"
      contains: "def test_"
  key_links:
    - from: "src/forexfactory/cli.py"
      to: "forexfactory._populate.run_populate / forexfactory._query.run_query"
      via: "subcommand handlers"
      pattern: "run_populate|run_query"
    - from: "src/forexfactory/cli.py (query handler)"
      to: "stdout"
      via: "print(path) — path only (D-10)"
      pattern: "print\\("
---

<objective>
Wire the unified `forexfactory` console script (PKG-02) with `populate` and `query`
subcommands and prove the walking skeleton end-to-end: install → populate a real
month → query → read the parquet back. The CLI is the UI for this tool. `query`
prints path-only to stdout (D-10), scope uses repeatable append flags (D-12), and the
out-of-scope error exits non-zero (D-09).

Purpose: Makes ROADMAP Success Criteria 2 and 3 true and ties the skeleton together.
Output: `src/forexfactory/cli.py`, `tests/test_cli.py` (incl. the end-to-end smoke test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-installable-data-provider/01-CONTEXT.md
@.planning/phases/01-installable-data-provider/01-PATTERNS.md
@.planning/phases/01-installable-data-provider/01-SKELETON.md
@src/forexfactory/_populate.py
@src/forexfactory/_query.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Walking-skeleton end-to-end test (RED first)</name>
  <read_first>
    - src/forexfactory/_populate.py (run_populate signature + defaults)
    - src/forexfactory/_query.py (run_query signature + return)
    - tests/test_scrape.py:116-146 (the cli-main argv test pattern to mirror)
    - .planning/phases/01-installable-data-provider/01-SKELETON.md (the capability proven end-to-end)
  </read_first>
  <files>tests/test_cli.py</files>
  <behavior>
    - `SkeletonEndToEndTests`: write a temp raw dir with one real-shaped USD/high `days_2026_03.json`; call `cli.main(["populate", "--raw-dir", raw, "--cache-dir", cache])`; then call `cli.main(["query", "--currency", "USD", "--impact", "high", "--cache-dir", cache])` capturing stdout; assert stdout is a single absolute path that exists and that `pd.read_parquet(path)` has the six DATA-01 columns with USD/high rows.
    - The populate step makes zero HTTP calls.
  </behavior>
  <action>
    Create `tests/test_cli.py` and write the end-to-end skeleton test FIRST (it will be
    RED until Task 2 implements `cli.main`). Use `tempfile.TemporaryDirectory` for both a
    raw input dir and a cache dir, `contextlib.redirect_stdout`/`io.StringIO` (or
    `capsys`-style capture) to capture the query path, and `pathlib.Path`/`pandas` to
    assert the parquet opens with columns {datetime_utc, currency, impact, title, id,
    leaked}. Assert stdout from the `query` call is EXACTLY one line: an absolute path
    (D-10). Import `import forexfactory.cli as cli`.
  </action>
  <acceptance_criteria>
    - The file `tests/test_cli.py` defines `SkeletonEndToEndTests` with a populate→query→read-parquet flow.
    - Running it BEFORE Task 2 fails (RED) because `cli.main` is not yet implemented; after Task 2 it passes (GREEN).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_cli.py -q -k Skeleton</automated>
  </verify>
  <done>A failing end-to-end test pins the skeleton contract: install→populate→query→valid parquet path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cli.py (populate + query subcommands, dispatch, D-10/D-12/D-09)</name>
  <read_first>
    - scrape.py:395-442 (parse_args/main + sys.exit(1) on bad dates pattern)
    - pipeline.py:236-281 (subcommand dispatch pattern) — now src/forexfactory/_pipeline.py
    - .planning/phases/01-installable-data-provider/01-PATTERNS.md (section "src/forexfactory/cli.py")
    - .planning/phases/01-installable-data-provider/01-CONTEXT.md (D-04, D-09, D-10, D-12)
  </read_first>
  <files>src/forexfactory/cli.py, tests/test_cli.py</files>
  <behavior>
    - `cli.main(["query","--currency","USD","--currency","EUR",...])` yields `currencies == ["USD","EUR"]` (D-12 append).
    - `query` prints only the path on stdout; nothing else on stdout (D-10).
    - Omitting `--currency`/`--impact` applies the USD + high/holiday default (D-04).
    - An out-of-scope `query` exits non-zero (SystemExit code 1) with the guidance on stderr (D-09).
  </behavior>
  <action>
    Create `src/forexfactory/cli.py` (module docstring, logging, `from . import _populate, _query`).
    Build a top-level `argparse.ArgumentParser` with `add_subparsers(dest="command")` and
    two subparsers for now — `populate` and `query` (the `refresh` subparser is added in
    plan 06). For BOTH, add `--currency` and `--impact` with `action="append"` (D-12,
    default None → handlers apply the D-04 USD+high/holiday default), `--start` and
    `--end` (YYYY-MM strings), and `--cache-dir` (CACHE-01 override forwarded to the
    services). `populate` also gets `--raw-dir` (default `_populate.RAW_INPUT_DIR`).
    Define `def main(argv: list[str] | None = None) -> int:` (accept argv for testing, per
    the scrape.py pattern). Dispatch:
    populate → `_populate.run_populate(currencies=args.currency, impacts=args.impact, start=args.start, end=args.end, raw_dir=args.raw_dir, cache_dir=args.cache_dir)` and log the summary to stderr/logging;
    query → call `_query.run_query(...)`, then `print(path)` to STDOUT and nothing else on
    stdout (D-10) — log row count/scope to logger (stderr). Wrap the query call so a
    `ValueError` (D-09 out-of-scope) is caught, its message printed to `sys.stderr`, and
    `sys.exit(1)`. Validate `--start`/`--end` shape and `sys.exit(1)` on malformed input
    (mirror scrape.py date validation). Keep `if __name__ == "__main__": raise SystemExit(main())`.
    Then complete `tests/test_cli.py`: the Skeleton test now passes; add
    `CliRoutingTests` covering — `--currency USD --currency EUR` → `["USD","EUR"]` (patch
    `_query.run_query` to capture kwargs); query prints only the path to stdout; an
    out-of-scope query (patch run_query to raise ValueError) exits with code 1 and writes
    the message to stderr; populate routes to `_populate.run_populate` with forwarded args.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_cli.py -q` passes (skeleton + routing).
    - `forexfactory query --currency USD --impact high --cache-dir <populated>` prints exactly one absolute path line to stdout; `python -c "import pandas as pd,sys; pd.read_parquet(sys.argv[1])" "$(...)"` opens it.
    - A routing test asserts repeated `--currency` flags collect into a list (D-12).
    - A test asserts the out-of-scope path exits code 1 with `forexfactory populate` guidance on stderr (D-09).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_cli.py -q</automated>
  </verify>
  <done>`forexfactory populate` and `forexfactory query` work end-to-end; query is path-only on stdout; append flags, default scope, and the out-of-scope exit code are verified.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_cli.py -q` passes (incl. the end-to-end skeleton test).
- Manual: `pip install -e . && forexfactory populate --raw-dir out && forexfactory query --currency USD --impact high` prints a path that `pd.read_parquet` opens (SC2, SC3).
- Security note: no network here; only local file I/O and argument parsing. Result-path
  construction reuses the sanitized D-08 key from _query.
</verification>

<success_criteria>
- ROADMAP Success Criteria 2 and 3 are satisfied via the `forexfactory` CLI.
- The walking skeleton (install→populate→query→parquet path) is proven by an automated test.
- D-10/D-12/D-09 verified.
</success_criteria>

<output>
Create `.planning/phases/01-installable-data-provider/01-05-SUMMARY.md` when done.
</output>
