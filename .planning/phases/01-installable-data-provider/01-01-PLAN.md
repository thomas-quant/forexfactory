---
phase: 01-installable-data-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/forexfactory/__init__.py
  - src/forexfactory/_pipeline.py
  - tests/test_pipeline.py
autonomous: true
requirements: [PKG-01, PKG-03, PKG-04, QUAL-01, QUAL-02, DATA-01]
must_haves:
  truths:
    - "PKG-01 / D-13 / SC1: `pip install -e .` succeeds and `python -c \"import forexfactory\"` exits 0 in a fresh venv without importing scrape.py or pipeline.py"
    - "PKG-03 / SC4: `forexfactory.get` is a callable that is annotated to return `pathlib.Path`"
    - "PKG-04: the existing pipeline filter/flatten/parquet logic is reused (moved, not rewritten) as `forexfactory._pipeline`"
    - "QUAL-01: dedup logic exists in exactly one place — `_deduplicate_rows()` — called by both `parse_json_to_csv` and `run_pipeline`"
    - "QUAL-02: `run_pipeline(in_dir=...)` actually reads from the passed directory (no silent no-op)"
    - "DATA-01: `flatten_events` still emits the six core fields datetime_utc(date+time_utc), currency, impact, title, id, leaked"
  artifacts:
    - path: "pyproject.toml"
      provides: "Installable src-layout package metadata + console-script entry point"
      contains: "name = \"forexfactory\""
    - path: "src/forexfactory/__init__.py"
      provides: "Library namespace + get() -> Path contract"
      contains: "def get"
    - path: "src/forexfactory/_pipeline.py"
      provides: "Reused ETL engine with QUAL-01/QUAL-02 fixes"
      contains: "def _deduplicate_rows"
    - path: "tests/test_pipeline.py"
      provides: "Adapted + QUAL regression tests"
      contains: "import forexfactory._pipeline"
  key_links:
    - from: "pyproject.toml"
      to: "forexfactory.cli:main"
      via: "[project.scripts] console entry point"
      pattern: "forexfactory = \"forexfactory.cli:main\""
    - from: "src/forexfactory/__init__.py"
      to: "forexfactory._query"
      via: "lazy import inside get()"
      pattern: "from . import _query"
---

<objective>
Make the project a pip-installable `forexfactory` package (src layout) and move the
existing `pipeline.py` ETL engine into it as `forexfactory._pipeline`, paying down
QUAL-01 (duplicated dedup) and QUAL-02 (`--in-dir` silent no-op) in the process.

This is the first slice of the walking skeleton: after this plan a real user can
`pip install -e .` and `import forexfactory` (ROADMAP Success Criterion 1), and the
library `get()` contract exists for downstream plans to implement against.

Purpose: Establish the installable namespace and reuse (not rewrite) the proven
pipeline logic, per D-13 and PKG-04.
Output: `pyproject.toml`, `src/forexfactory/__init__.py`, `src/forexfactory/_pipeline.py`,
adapted `tests/test_pipeline.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-data-provider/01-CONTEXT.md
@.planning/phases/01-installable-data-provider/01-PATTERNS.md
@.planning/phases/01-installable-data-provider/01-SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml (src-layout installable package)</name>
  <read_first>
    - requirements.txt (exact dependency pins to mirror)
    - .planning/phases/01-installable-data-provider/01-PATTERNS.md (section "pyproject.toml")
    - .planning/phases/01-installable-data-provider/01-CONTEXT.md (D-13 package identity)
    - CLAUDE.md (Python 3.12+, stack constraints)
  </read_first>
  <files>pyproject.toml</files>
  <action>
    Create `pyproject.toml` at repo root using the setuptools build backend
    (`[build-system] requires = ["setuptools>=65.0.0"]`, `build-backend = "setuptools.build_meta"`).
    Under `[project]` set `name = "forexfactory"` (distribution name identical to the
    import name, per D-13), `version = "0.1.0"`, `requires-python = ">=3.12"`, and
    `dependencies` mirroring requirements.txt exactly: `curl_cffi>=0.13.0`,
    `pandas>=2.0.0`, `pyarrow>=14.0.0`. (setuptools is the build requirement, not a
    runtime dep.) Add `[project.scripts]` with the single console script
    `forexfactory = "forexfactory.cli:main"` — one entry point, no `ff` alias (D-13).
    Configure src layout with `[tool.setuptools.packages.find] where = ["src"]`.
    Do NOT add PyPI classifiers/URLs (DIST-01 deferred to v2). The entry point may
    reference `forexfactory.cli:main` even though cli.py is created in a later plan —
    entry points resolve lazily at run time, so `pip install -e .` still succeeds.
  </action>
  <acceptance_criteria>
    - `pip install -e .` exits 0 in a fresh venv (after Task 2 creates __init__.py).
    - `grep 'name = "forexfactory"' pyproject.toml` matches.
    - `grep 'forexfactory = "forexfactory.cli:main"' pyproject.toml` matches.
    - `grep 'where = \["src"\]' pyproject.toml` matches.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['name']=='forexfactory'; assert d['project']['scripts']['forexfactory']=='forexfactory.cli:main'; assert d['tool']['setuptools']['packages']['find']['where']==['src']"</automated>
  </verify>
  <done>pyproject.toml declares the forexfactory distribution, src layout, console script, and the three runtime deps.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/forexfactory/__init__.py with the get() -> Path contract</name>
  <read_first>
    - .planning/phases/01-installable-data-provider/01-PATTERNS.md (section "src/forexfactory/__init__.py")
    - scrape.py lines 1-9 (module docstring style) and 22-25 (conditional-import style)
    - .planning/codebase/CONVENTIONS.md (no __all__; pathlib.Path return types)
    - .planning/phases/01-installable-data-provider/01-CONTEXT.md (D-07 get() returns a parquet path)
  </read_first>
  <files>src/forexfactory/__init__.py</files>
  <action>
    Create `src/forexfactory/__init__.py`. Add a module docstring describing the
    library and a usage example (`from forexfactory import get; path = get(currencies=["USD"], impacts=["high"])`).
    Import `from pathlib import Path`. Define the public library entry point
    `def get(*, currencies=None, impacts=None, start=None, end=None, cache_dir=None) -> Path:`
    (keyword-only optional args per CONVENTIONS). Its body must LAZILY import the query
    service inside the function (`from . import _query`) and return
    `_query.run_query(currencies=currencies, impacts=impacts, start=start, end=end, cache_dir=cache_dir)`.
    The lazy import is deliberate (interface-first): `import forexfactory` must succeed
    NOW even though `_query.py` is built in plan 04 — `get()` only resolves `_query`
    when actually called. Do not define `__all__` (project convention). Add a
    `__version__ = "0.1.0"` string.
  </action>
  <acceptance_criteria>
    - `python -c "import forexfactory; assert callable(forexfactory.get)"` exits 0.
    - `python -c "import forexfactory, inspect; assert inspect.signature(forexfactory.get).return_annotation in ('Path', __import__('pathlib').Path)"` exits 0.
    - The string `from . import _query` appears inside the get() body.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import forexfactory; assert callable(forexfactory.get); assert forexfactory.__version__"</automated>
  </verify>
  <done>`import forexfactory` works (SC1) and `forexfactory.get` is defined with a Path return annotation, lazily delegating to _query.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Relocate pipeline.py -> _pipeline.py with QUAL-01 + QUAL-02 fixes</name>
  <read_first>
    - pipeline.py (entire file — the engine being moved)
    - tests/test_pipeline.py (the suite being adapted)
    - .planning/codebase/CONCERNS.md ("Duplicated deduplication logic", "run_pipeline() ignores ... in_dir")
    - .planning/phases/01-installable-data-provider/01-PATTERNS.md (section "src/forexfactory/_pipeline.py", "QUAL Fix Summary")
  </read_first>
  <files>src/forexfactory/_pipeline.py, tests/test_pipeline.py</files>
  <behavior>
    - `_deduplicate_rows(rows)`: given two input dicts with the same id/date/time_utc, returns one row; result is sorted by (date, time_utc, title).
    - `run_pipeline(out_parquet, in_dir=tmp)`: reads days_*.json from the PASSED in_dir, not the module global — a month written only under the passed in_dir appears in the output DataFrame.
    - `run_pipeline` honors `keep_currencies`/`keep_impacts` kw args; rows outside the set are excluded.
    - `flatten_events` still yields the six DATA-01 fields (date, time_utc, currency, impact, title, id, leaked).
  </behavior>
  <action>
    `git mv pipeline.py src/forexfactory/_pipeline.py` to preserve history, then edit:
    (QUAL-01) Extract the copy-pasted dedup block (currently duplicated in
    `parse_json_to_csv` and `run_pipeline`) into a module-level helper
    `def _deduplicate_rows(rows: list[dict]) -> list[dict]:` that builds the dedup dict
    keyed `(id, date, time_utc)` when id is truthy else `(date, time_utc, currency, title)`,
    returns the values sorted by `(date, time_utc, title)`. Call `rows = _deduplicate_rows(rows)`
    in BOTH `parse_json_to_csv` and `run_pipeline`.
    (QUAL-02) Change `run_pipeline` signature to
    `def run_pipeline(out_parquet: str = OUT_PARQUET, *, in_dir: str = IN_DIR, keep_currencies: set = KEEP_CURRENCIES, keep_impacts: set = KEEP_IMPACTS) -> None:` and use the
    `in_dir`/`keep_currencies`/`keep_impacts` PARAMETERS in the body (replace the
    `load_days_files(IN_DIR)` global read and the `KEEP_CURRENCIES`/`KEEP_IMPACTS` global
    filters). Keep `flatten_events`, `to_iso`, `norm_impact`, `write_parquet`,
    `should_keep_row`, `csv_to_parquet`, `sanitize_csv`, and the config constants
    unchanged so downstream plans reuse them. Leave the `if __name__ == "__main__"`/`main()`
    in place for now (the package CLI is plan 05; this module stays importable).
    Delete the root `pipeline.py` is handled by `git mv` (it no longer exists at root).
    Then adapt tests/test_pipeline.py: change `import pipeline` to
    `import forexfactory._pipeline as pipeline`; in `test_run_pipeline_uses_zstd_level_3`
    and `test_run_pipeline_preserves_leaked_in_parquet_dataframe`, REMOVE the
    `patch.object(pipeline, "IN_DIR", str(in_dir))` wrapper and instead pass
    `in_dir=str(in_dir)` to `run_pipeline(...)` (proves QUAL-02). Add two new tests in a
    `PipelineDedupTests(unittest.TestCase)` class: one asserting `_deduplicate_rows`
    collapses duplicate-id rows to one and sorts; one asserting `run_pipeline(in_dir=...)`
    reads from the passed dir (write a fixture month only there and assert it reaches
    `to_parquet`). Do NOT modify README.md or test_docs.py (owned by plan 07).
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_pipeline.py -q` passes (all adapted + new tests).
    - `grep -c "def _deduplicate_rows" src/forexfactory/_pipeline.py` returns 1.
    - `grep -c "_deduplicate_rows(rows)" src/forexfactory/_pipeline.py` returns 2 (called in both paths).
    - `python -c "import forexfactory._pipeline as p, inspect; assert 'in_dir' in inspect.signature(p.run_pipeline).parameters"` exits 0.
    - root `pipeline.py` no longer exists (`test ! -f pipeline.py`).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_pipeline.py -q</automated>
  </verify>
  <done>The ETL engine lives at src/forexfactory/_pipeline.py with a single shared dedup helper (QUAL-01) and a working in_dir parameter (QUAL-02); the adapted suite passes.</done>
</task>

</tasks>

<verification>
- `pip install -e .` exits 0; `python -c "import forexfactory"` exits 0 (SC1, PKG-01).
- `python -m pytest tests/test_pipeline.py -q` passes.
- No code imports the old root `scrape.py`/`pipeline.py`; the package is self-contained for the pipeline engine.
- Security note: this plan introduces no external trust boundary (no network, no untrusted input); it only relocates pure local-file ETL code.
</verification>

<success_criteria>
- ROADMAP Success Criterion 1 is satisfied: install + import work in a fresh environment without importing the loose scripts.
- QUAL-01 and QUAL-02 are closed (verified by grep + tests).
- The `get() -> Path` contract exists for plans 04/05 to implement against.
</success_criteria>

<output>
Create `.planning/phases/01-installable-data-provider/01-01-SUMMARY.md` when done.
</output>
