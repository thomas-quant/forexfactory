---
phase: 01-installable-data-provider
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/forexfactory/_query.py
  - tests/test_query.py
autonomous: true
requirements: [PKG-02, PKG-03, CACHE-02, DATA-01]
must_haves:
  truths:
    - "D-07 / PKG-03 / SC4: `run_query(...)` reads the relevant per-month cache parquet, applies the currency/impact/date filter, writes ONE consolidated parquet, and returns its absolute `pathlib.Path`; `forexfactory.get(...)` returns that Path"
    - "D-08: the result parquet is written to a deterministic, filter-keyed path under `cache_dir/queries/` and overwritten on every call"
    - "D-09 / CACHE-02: an out-of-scope request (manifest scope does not cover it) raises a ValueError with actionable populate guidance — no silent partial/empty result"
    - "DATA-01: the consolidated parquet opens with `pd.read_parquet` and exposes datetime_utc, currency, impact, title, id, leaked"
  artifacts:
    - path: "src/forexfactory/_query.py"
      provides: "Cache read → filter → consolidated result parquet"
      contains: "def run_query"
    - path: "tests/test_query.py"
      provides: "Result-path, get()-returns-Path, and D-09 out-of-scope tests"
      contains: "def test_"
  key_links:
    - from: "forexfactory.get"
      to: "forexfactory._query.run_query"
      via: "lazy import in __init__.get()"
      pattern: "run_query"
    - from: "src/forexfactory/_query.py"
      to: "cache_dir/queries/*.parquet"
      via: "_pipeline.write_parquet to a deterministic _cache.queries_dir path"
      pattern: "queries_dir"
---

<objective>
Build the query slice — the real cache READ half of the walking skeleton. It reads
the per-month cache parquet for the requested range, filters by currency/impact,
writes ONE consolidated result parquet to a deterministic path (D-07/D-08), and
returns its absolute `pathlib.Path`. `forexfactory.get(...)` (the library contract
from plan 01) is wired through to it (PKG-03, SC4). Out-of-scope requests fail loud
with populate guidance (D-09).

Purpose: After this plan the library `get()` works end-to-end against a populated
cache.
Output: `src/forexfactory/_query.py`, `tests/test_query.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-installable-data-provider/01-CONTEXT.md
@.planning/phases/01-installable-data-provider/01-PATTERNS.md
@.planning/phases/01-installable-data-provider/01-SKELETON.md
@src/forexfactory/_pipeline.py
@src/forexfactory/_cache.py
@src/forexfactory/__init__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_query — read cache, filter, write consolidated result parquet (D-07/D-08)</name>
  <read_first>
    - src/forexfactory/_cache.py (resolve_cache_dir, month_parquet_path, queries_dir, read_manifest, _scope_covers, ensure_dirs)
    - src/forexfactory/_pipeline.py (write_parquet — reuse the project compression settings)
    - src/forexfactory/__init__.py (the get() lazy wrapper this implements)
    - .planning/phases/01-installable-data-provider/01-PATTERNS.md (section "src/forexfactory/_query.py")
    - .planning/phases/01-installable-data-provider/01-CONTEXT.md (D-07, D-08)
  </read_first>
  <files>src/forexfactory/_query.py</files>
  <behavior>
    - Given cache parquet files for 2026-01..2026-03 and a USD/high request, `run_query(currencies=["USD"], impacts=["high"], cache_dir=tmp)` returns a `pathlib.Path` under `tmp/queries/` that exists.
    - `pd.read_parquet(result)` has the DATA-01 columns and only USD/high rows.
    - Two successive identical calls return the SAME path (deterministic, D-08) and overwrite it.
    - `forexfactory.get(currencies=["USD"], impacts=["high"], cache_dir=tmp)` returns the same Path (SC4).
  </behavior>
  <action>
    Create `src/forexfactory/_query.py` (module docstring, logging, keyword-only args,
    `from pathlib import Path`). Implement
    `run_query(*, currencies=None, impacts=None, start=None, end=None, cache_dir=None) -> Path`:
    default scope to USD + {high, holiday} when None (consistent with populate D-04);
    `cache_dir = _cache.resolve_cache_dir(cache_dir)`, `_cache.ensure_dirs(cache_dir)`.
    Read the manifest; determine the candidate months = manifest `months` keys narrowed
    to [start, end] when given (else all cached months). Read each existing
    `_cache.month_parquet_path(cache_dir, anchor)` with `pd.read_parquet`, skip months
    whose parquet is absent, and `pd.concat` the frames (empty → an empty DataFrame with
    the DATA-01 columns). Filter: `df[df["currency"].isin(currencies) & df["impact"].isin(impacts)]`.
    Compute the deterministic result path (D-08): a filter-keyed filename under
    `_cache.queries_dir(cache_dir)`, e.g.
    `f"{'-'.join(sorted(currencies))}__{'-'.join(sorted(impacts))}__{start or 'all'}_{end or 'all'}.parquet"`
    (sanitize so it is a safe single filename). Write it with
    `_pipeline.write_parquet(filtered_df, str(result_path))` (overwrite each call) and
    return `Path(result_path).resolve()` (D-07). Send all diagnostics (row count, scope,
    range) to `logger`, never to stdout — the CLI owns stdout (D-10, plan 05). Do not
    modify __init__.py (plan 01 already wired the lazy `get()` → `run_query`).
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_query.py -q` passes.
    - A test writes fake per-month parquet via `_cache.month_parquet_path`, calls `run_query`, and asserts the returned value `isinstance(result, pathlib.Path)`, `result.exists()`, and `result.parent == _cache.queries_dir(cache_dir)`.
    - A test asserts `forexfactory.get(...)` returns the identical Path (SC4 / PKG-03).
    - `pd.read_parquet(result).columns` ⊇ {datetime_utc, currency, impact, title, id, leaked} and contains only requested currency/impact rows.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_query.py -q</automated>
  </verify>
  <done>run_query returns a deterministic absolute Path to a consolidated, filtered parquet; forexfactory.get() resolves through to it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-09 out-of-scope query error with populate guidance</name>
  <read_first>
    - src/forexfactory/_query.py (run_query from Task 1)
    - src/forexfactory/_cache.py (read_manifest, _scope_covers)
    - .planning/phases/01-installable-data-provider/01-CONTEXT.md (D-09 — error with guidance, no silent partial)
    - .planning/phases/01-installable-data-provider/01-PATTERNS.md ("What query does" — the D-09 message format)
  </read_first>
  <files>src/forexfactory/_query.py, tests/test_query.py</files>
  <behavior>
    - Requesting EUR/medium when the manifest scope is only USD/high raises `ValueError` whose message names the missing scope and the exact remediation command, e.g. "EUR/medium not populated — run: forexfactory populate --currency EUR --impact medium".
    - No result parquet is written when the request is out of scope (loud failure, no silent empty file).
  </behavior>
  <action>
    At the top of `run_query` (before reading month parquet), load the manifest scope and,
    for the requested currencies/impacts, check `_cache._scope_covers(scope, currencies, impacts)`.
    If the manifest is empty (nothing populated) or the scope does not cover the request,
    raise `ValueError` with an actionable message that lists the uncovered currency/impact
    pair(s) and the exact `forexfactory populate --currency <C> --impact <I>` command to
    fix it (D-09). Do not write any result file in that path. Add tests asserting the
    ValueError is raised with a message containing both the uncovered scope and the
    `forexfactory populate` remediation, and that no file is created under `queries/`.
    (The CLI in plan 05 catches this ValueError and exits non-zero.)
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_query.py -q` passes including the D-09 case.
    - A test asserts `run_query` raises `ValueError` for an out-of-scope request and that `queries_dir` contains no new file afterward.
    - The raised message contains the substring `forexfactory populate`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_query.py -q</automated>
  </verify>
  <done>Out-of-scope queries fail loudly with actionable guidance and produce no partial output (D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library/CLI caller → run_query filter args | currencies/impacts/start/end flow into a result filename |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering (path traversal) | deterministic result filename from filter args | mitigate | Join only sanitized scope tokens (uppercase currency / known impact words) into a single filename under `queries_dir`; never interpolate raw separators/`..` |
| T-01-04 | Information disclosure | empty/partial results on scope miss | mitigate | D-09 raises before any read/write; no silent partial parquet is produced |
</threat_model>

<verification>
- `python -m pytest tests/test_query.py -q` passes.
- `forexfactory.get(currencies=["USD"], impacts=["high"])` returns a `pathlib.Path` that `pd.read_parquet` opens (SC4).
</verification>

<success_criteria>
- SC4 satisfied: library `get()` returns a parquet Path.
- D-07/D-08/D-09 exercised by tests; result path is deterministic and out-of-scope fails loud.
</success_criteria>

<output>
Create `.planning/phases/01-installable-data-provider/01-04-SUMMARY.md` when done.
</output>
