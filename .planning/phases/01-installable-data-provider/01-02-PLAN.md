---
phase: 01-installable-data-provider
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/forexfactory/_cache.py
  - tests/test_cache.py
autonomous: true
requirements: [CACHE-01]
must_haves:
  truths:
    - "CACHE-01: the cache dir defaults to `~/.cache/forexfactory` and is overridable via the `FOREXFACTORY_CACHE_DIR` env var and an explicit `cache_dir` argument"
    - "D-01: there is a `month_parquet_path(cache_dir, anchor)` that resolves one parquet file per calendar month (e.g. `2024-03.parquet`)"
    - "D-02: a `manifest.json` sidecar at the cache root records populated scope (currencies/impacts) and per-month provenance (scraped_at, settled); `read_manifest` returns `{}` when absent"
    - "D-03: raw per-month JSON is staged under `cache_dir/raw/` via `raw_json_path(cache_dir, anchor)`"
    - "D-08: query result parquets live under `cache_dir/queries/` via `queries_dir(cache_dir)`"
  artifacts:
    - path: "src/forexfactory/_cache.py"
      provides: "Cache path resolution + manifest read/write/scope helpers"
      contains: "FOREXFACTORY_CACHE_DIR"
    - path: "tests/test_cache.py"
      provides: "Round-trip + default-dir + env-override regression tests"
      contains: "def test_"
  key_links:
    - from: "src/forexfactory/_cache.py"
      to: "manifest.json"
      via: "atomic write_manifest using os.replace"
      pattern: "os\\.replace"
    - from: "src/forexfactory/_cache.py"
      to: "FOREXFACTORY_CACHE_DIR"
      via: "os.environ override of DEFAULT_CACHE_DIR"
      pattern: "os\\.environ"
---

<objective>
Build the cache layer that every later slice depends on: deterministic path
resolution for per-month parquet (D-01), the raw JSON staging dir (D-03), the query
result dir (D-08), and the `manifest.json` sidecar (D-02) that records populated
scope and per-month provenance. The cache home is `~/.cache/forexfactory` and is
overridable (CACHE-01).

Purpose: One module owns all cache filesystem layout + the manifest contract, so
populate, query, and refresh never hard-code paths.
Output: `src/forexfactory/_cache.py`, `tests/test_cache.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-installable-data-provider/01-CONTEXT.md
@.planning/phases/01-installable-data-provider/01-PATTERNS.md
@.planning/phases/01-installable-data-provider/01-SKELETON.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _cache.py (paths + manifest + scope helpers)</name>
  <read_first>
    - .planning/phases/01-installable-data-provider/01-PATTERNS.md (section "src/forexfactory/_cache.py" incl. the manifest schema and the helper list)
    - .planning/phases/01-installable-data-provider/01-CONTEXT.md (D-01, D-02, D-03, D-08; "Integration Points" cache layout)
    - pipeline.py:38-48 (load_days_files — json read + warn-and-skip pattern) — now at src/forexfactory/_pipeline.py
    - scrape.py:360,368 (os.makedirs / path construction patterns)
  </read_first>
  <files>src/forexfactory/_cache.py</files>
  <behavior>
    - `resolve_cache_dir()` with no arg returns `~/.cache/forexfactory`; with `FOREXFACTORY_CACHE_DIR` set returns that path; with an explicit `cache_dir` arg returns that (explicit arg wins over env wins over default).
    - `read_manifest(cache_dir)` on a missing file returns `{}` (no FileNotFoundError).
    - `write_manifest` then `read_manifest` round-trips scope + a month entry unchanged.
    - `month_parquet_path(cache_dir, date(2024,3,1))` ends with `2024-03.parquet`.
    - `raw_json_path(cache_dir, date(2024,3,1))` ends with `raw/days_2024_03.json`.
    - `_scope_covers({"currencies":["USD","EUR"],"impacts":["high"]}, ["USD"], ["high"])` is True; requesting EUR/medium against a USD/high scope is False.
  </behavior>
  <action>
    Create `src/forexfactory/_cache.py` following the project conventions (module
    docstring, logging setup, a `# ====== CONFIG ======` block, keyword-only optional
    args, `from pathlib import Path`, `from datetime import date`). Define
    `DEFAULT_CACHE_DIR = Path.home() / ".cache" / "forexfactory"` and the env var name
    constant `CACHE_DIR_ENV = "FOREXFACTORY_CACHE_DIR"`. Implement:
    `resolve_cache_dir(cache_dir=None) -> Path` (precedence: explicit arg > os.environ[CACHE_DIR_ENV] > DEFAULT_CACHE_DIR; always returns a Path) — this is the single CACHE-01 override point;
    `raw_dir(cache_dir) -> Path` = `cache_dir/"raw"` (D-03);
    `queries_dir(cache_dir) -> Path` = `cache_dir/"queries"` (D-08);
    `month_parquet_path(cache_dir, anchor: date) -> Path` = `cache_dir / f"{anchor:%Y-%m}.parquet"` (D-01);
    `raw_json_path(cache_dir, anchor: date) -> Path` = `raw_dir(cache_dir) / f"days_{anchor:%Y_%m}.json"` (matches the legacy `days_YYYY_MM.json` name);
    `manifest_path(cache_dir) -> Path` = `cache_dir/"manifest.json"`;
    `ensure_dirs(cache_dir) -> None` (os.makedirs cache_dir, raw_dir, queries_dir with exist_ok=True);
    `read_manifest(cache_dir) -> dict` (returns `{}` if file missing or json invalid — warn-and-skip per the load_days_files pattern; never raise);
    `write_manifest(cache_dir, manifest: dict) -> None` (ensure_dirs, write to a temp file then `os.replace` for atomicity);
    `update_manifest_month(cache_dir, anchor, *, scraped_at: str, settled: bool, currencies: list, impacts: list) -> dict` (reads, sets `manifest["scope"] = {"currencies": sorted(currencies), "impacts": sorted(impacts)}` as the union/last-write of populated scope, sets `manifest.setdefault("months", {})[f"{anchor:%Y-%m}"] = {"scraped_at": scraped_at, "settled": settled}`, writes, returns the manifest);
    `_scope_covers(scope: dict, currencies: list, impacts: list) -> bool` (True iff every requested currency and impact is in the manifest scope lists). Match the D-02 manifest schema from PATTERNS.md exactly (top-level `scope` and `months` keys).
  </action>
  <acceptance_criteria>
    - `python -c "from forexfactory import _cache; from datetime import date; assert str(_cache.month_parquet_path(__import__('pathlib').Path('/c'), date(2024,3,1))).endswith('2024-03.parquet')"` exits 0.
    - `python -c "from forexfactory import _cache; assert _cache.read_manifest(__import__('pathlib').Path('/nonexistent-xyz'))=={}"` exits 0.
    - With `FOREXFACTORY_CACHE_DIR=/tmp/ffc`, `resolve_cache_dir()` returns `/tmp/ffc`.
    - `_scope_covers({"currencies":["USD"],"impacts":["high"]}, ["EUR"], ["high"])` is False.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_cache.py -q</automated>
  </verify>
  <done>_cache.py resolves all cache paths, round-trips the manifest atomically, honors CACHE-01 overrides, and exposes scope-coverage logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_cache.py</name>
  <read_first>
    - tests/test_pipeline.py:42-75 (tempdir + fixture-writing pattern to mirror)
    - .planning/phases/01-installable-data-provider/01-PATTERNS.md (section "tests/test_cache.py" — the checklist of cases)
  </read_first>
  <files>tests/test_cache.py</files>
  <action>
    Create `tests/test_cache.py` with a `CacheTests(unittest.TestCase)` class using
    `tempfile.TemporaryDirectory` (mirror the test_pipeline tempdir style) and
    `unittest.mock.patch.dict(os.environ, ...)` for the env-override case. Cover:
    read_manifest returns `{}` on missing file; write_manifest + read_manifest round-trips
    scope and a month entry; month_parquet_path / raw_json_path return the expected
    `YYYY-MM.parquet` and `raw/days_YYYY_MM.json` patterns; DEFAULT_CACHE_DIR resolves to
    `~/.cache/forexfactory`; resolve_cache_dir honors FOREXFACTORY_CACHE_DIR and an
    explicit arg over the env; ensure_dirs creates raw/ and queries/ subdirs;
    update_manifest_month records scraped_at + settled and sets scope; _scope_covers
    True/False cases. Import `from forexfactory import _cache`.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_cache.py -q` passes with at least 7 test methods.
    - `grep -c "def test_" tests/test_cache.py` is >= 7.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_cache.py -q</automated>
  </verify>
  <done>The cache layer has regression coverage for paths, manifest round-trip, env override, and scope coverage.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_cache.py -q` passes.
- Security note: no external trust boundary. The only untrusted-ish input is an
  existing on-disk `manifest.json`; `read_manifest` degrades to `{}` on corrupt/missing
  files rather than raising or trusting partial content.
</verification>

<success_criteria>
- CACHE-01 satisfied: default `~/.cache/forexfactory` with env + arg override, verified by test.
- The manifest contract (D-02) and per-month/raw/queries path scheme (D-01/D-03/D-08) are in place for plans 03, 04, 06.
</success_criteria>

<output>
Create `.planning/phases/01-installable-data-provider/01-02-SUMMARY.md` when done.
</output>
