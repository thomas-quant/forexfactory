---
phase: 01-installable-data-provider
plan: 07
type: execute
wave: 6
depends_on: ["01-06"]
files_modified:
  - README.md
  - tests/test_docs.py
  - api.txt
autonomous: true
requirements: [PKG-01, PKG-02, DATA-01]
must_haves:
  truths:
    - "D-13 / PKG-01: the README documents the package install + the single `forexfactory` console script (populate/refresh/query), and the structure chart shows the `src/forexfactory/` layout + `pyproject.toml`"
    - "PKG-02 / D-10: the README quickstart documents the full local flow `pip install -e .` → `forexfactory populate` → `forexfactory query --currency USD --impact high`, including capturing the path-only stdout"
    - "DATA-01: the README schema table documents datetime_utc, currency, impact, title, id, leaked"
    - "the doc-regression suite (tests/test_docs.py) is updated to assert the new layout/schema and passes; the stale api.txt scratch note is removed"
  artifacts:
    - path: "README.md"
      provides: "Package-era quickstart, CLI usage, schema table, src-layout structure chart"
      contains: "pip install -e ."
    - path: "tests/test_docs.py"
      provides: "Doc-regression tests matching the new structure + schema"
      contains: "src/forexfactory"
  key_links:
    - from: "tests/test_docs.py"
      to: "README.md"
      via: "structure-chart + schema substring assertions"
      pattern: "src/forexfactory"
---

<objective>
Bring the user-facing docs and the doc-regression suite in line with the shipped
package: rewrite the README quickstart for the install→populate→query flow, document
the `forexfactory` CLI (populate/refresh/query) and the path-only stdout contract,
update the schema table for the DATA-01 core fields and the structure chart for the
`src/forexfactory/` layout, refresh `tests/test_docs.py` to match, and delete the
stale `api.txt` scratch note.

Purpose: Finalize Phase 1 with accurate docs and a green doc-regression gate.
Output: updated `README.md`, updated `tests/test_docs.py`, removed `api.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-installable-data-provider/01-CONTEXT.md
@.planning/phases/01-installable-data-provider/01-PATTERNS.md
@README.md
@tests/test_docs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README for the packaged provider (install/CLI/schema/structure)</name>
  <read_first>
    - README.md (current content — preserve what is still true; replace the loose-script flow)
    - .planning/phases/01-installable-data-provider/01-CONTEXT.md (D-04, D-05, D-09, D-10, D-11, D-12, D-13 — CLI surface + defaults to document)
    - .planning/phases/01-installable-data-provider/01-PATTERNS.md (section "tests/test_docs.py" — the adaptations needed)
    - src/forexfactory/cli.py (the actual subcommands/flags to document)
  </read_first>
  <files>README.md</files>
  <action>
    Rewrite README.md for the package era. Replace the "Quick Start" loose-script steps
    with: (1) install — `pip install -e .`; (2) populate — `forexfactory populate`
    (re-processes the existing on-disk `out/` months into `~/.cache/forexfactory/` with no
    HTTP; default scope USD + high/holiday per D-04, default range = all on-disk months
    per D-05; widen with repeatable `--currency`/`--impact` flags per D-12; `--cache-dir`
    overrides the cache home per CACHE-01); (3) query — `forexfactory query --currency USD
    --impact high`, documenting that it prints ONLY the parquet path to stdout so it is
    script-friendly (show `PARQUET=$(forexfactory query --currency USD --impact high)`,
    D-10) and that an out-of-scope query errors with populate guidance (D-09); (4) refresh
    — `forexfactory refresh` fetches not-yet-cached months over the network with a polite
    default delay (D-11). Update the schema table to the DATA-01 columns (datetime_utc,
    currency, impact, title, id, leaked). Replace the "Project Structure" chart with the
    new `src/forexfactory/` src layout (list `pyproject.toml`, `src/`, `src/forexfactory/`
    with its modules, `tests/`, `README.md`, `requirements.txt`) and DROP the removed
    `scrape.py`, `pipeline.py`, and `api.txt` entries. Keep the data-source/license notes.
    Make the new structure chart and schema table the source of truth that Task 2's
    assertions will match exactly.
  </action>
  <acceptance_criteria>
    - README contains `pip install -e .`, `forexfactory populate`, `forexfactory query --currency USD --impact high`, and `forexfactory refresh`.
    - The schema table row for `leaked` is present and the table lists datetime_utc, currency, impact, title, id, leaked.
    - The structure chart references `src/forexfactory/` and `pyproject.toml` and no longer lists `api.txt`, root `scrape.py`, or root `pipeline.py`.
  </acceptance_criteria>
  <verify>
    <automated>python -c "t=open('README.md').read(); assert 'pip install -e .' in t and 'forexfactory query --currency USD --impact high' in t and 'src/forexfactory' in t and 'api.txt' not in t"</automated>
  </verify>
  <done>README documents the install→populate→query→refresh flow, the DATA-01 schema, and the src-layout structure.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests/test_docs.py + remove api.txt</name>
  <read_first>
    - tests/test_docs.py (current assertions to update)
    - README.md (the rewritten content from Task 1 — assertions must match it)
    - .planning/codebase/CONCERNS.md ("api.txt is an uncommitted scratch note" — remove it)
  </read_first>
  <files>tests/test_docs.py, api.txt</files>
  <action>
    Update `tests/test_docs.py` so the structure-chart test asserts the NEW layout —
    presence of `pyproject.toml`, `src/`, `src/forexfactory/` (and at least the package
    `__init__.py`/`cli.py` lines as written in the README chart), `tests/`, and the test
    files — and REMOVE the now-false assertions for `|-- api.txt`, `|-- scrape.py`, and
    `|-- pipeline.py`. Keep the schema test asserting the `leaked` boolean row (DATA-01),
    and ensure every substring it asserts is actually present in the rewritten README.
    Then delete the stale scratch note: `git rm --ignore-unmatch api.txt` (CONCERNS — it
    has no code consumer and misleads contributors; `--ignore-unmatch` keeps the task
    green even if the file is already gone or untracked).
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_docs.py -q` passes against the rewritten README.
    - `tests/test_docs.py` no longer asserts `api.txt`, root `scrape.py`, or root `pipeline.py` lines.
    - `api.txt` no longer exists (`test ! -f api.txt`).
    - Full suite green: `python -m pytest tests/ -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <done>The doc-regression suite matches the shipped package and passes; the stale api.txt is removed; the whole test suite is green.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` passes (entire Phase 1 suite).
- README accurately documents the packaged CLI + library flow and the DATA-01 schema.
- Security note: docs-only + file removal; no trust boundary touched.
</verification>

<success_criteria>
- PKG-01/PKG-02/DATA-01 are documented and asserted by the doc-regression tests.
- Phase 1 ships with accurate docs and a fully green test suite; api.txt removed.
</success_criteria>

<output>
Create `.planning/phases/01-installable-data-provider/01-07-SUMMARY.md` when done.
</output>
